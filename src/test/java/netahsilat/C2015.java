package netahsilat;

import io.testproject.addon.CombinedActions;
import io.testproject.addon.JSActions;
import io.testproject.addon.VisibleElementsOperations;
import io.testproject.addon.WebExtensions;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C2015 Ön bilgilendirme formu, Form Okuma Zorunluluğu ( %) parametresi aktif/pasif kontrolü
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Fri Oct 07 11:16:38 GMT 2022.
 */
@DisplayName("C2015 Ön bilgilendirme formu, Form Okuma Zorunluluğu ( %) parametresi aktif/pasif kontrolü")
public class C2015 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C2015 Ön bilgilendirme formu, Form Okuma Zorunluluğu ( %) parametresi aktif/pasif kontrolü")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String userName, String password, String kartNo, String CVV,
      String ayYil) throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogoutlogin methodlogoutlogin;
    WebExtensions.SelectOptionbyVisibleText selectOptionbyVisibleText;
    CombinedActions.ClearAndTypeAction clearAndTypeAction;
    JSActions.ClickAction clickAction;
    WebExtensions.ClickAndHold clickAndHold;
    WebExtensions.ReleaseClick releaseClick;
    VisibleElementsOperations.ClickIfVisibleWeb clickIfVisibleWeb;
    JSActions.ScrollToElementAction scrollToElementAction;

    // 1. Run Method Logout&Login
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = "otomasyon@musteri.temsilcisi";
    methodlogoutlogin.password = ProjectParameters.genelPassword;
    methodlogoutlogin.execute(driver);

    // 2. Move mouse to 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 3. Click 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    driver.findElement(by).click();

    // 4. Click 'btnParametreler'
    by = By.cssSelector("a[title='PARAMETRELER']");
    driver.findElement(by).click();

    // 5. Click 'li > a > Ön Bilgilendirme Formu'
    by = By.xpath("//a[normalize-space()='Ön Bilgilendirme Formu']");
    driver.findElement(by).click();

    // 6. Run Clear 'PreInformationForm.ScrollRequired' contents and type '100'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("100");
    by = By.cssSelector("#PreInformationForm_ScrollRequired");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 7. Select options in 'default-pi-status-options' with text '[NONE]'
    selectOptionbyVisibleText = WebExtensions.selectOptionbyVisibleText("Aktif");
    by = By.cssSelector("#default-pi-status-options");
    selectOptionbyVisibleText = (WebExtensions.SelectOptionbyVisibleText)((ReportingDriver)driver).addons().execute(selectOptionbyVisibleText, by, -1);

    // 8. Is 'Site sahibi (Checked) - Ödeme Talimat Formu' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//label[normalize-space()='Site sahibi']/../..//following-sibling::div//span[@class='jquery-safari-checkbox']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 9. Click 'Site sahibi (Checked) - Ödeme Talimat Formu' if it's visible
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("0");
    by = By.xpath("//label[normalize-space()='Site sahibi']/../..//following-sibling::div//span[@class='jquery-safari-checkbox']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 10. Is 'editorToolbar' visible?
    by = By.xpath("//div[contains(@id,'toolbargroup') and not(contains(@id,'Locales' ))]");
    driver.findElement(by).isDisplayed();

    // 11. Click 'Kaydet1'
    by = By.cssSelector("#kaydet");
    driver.findElement(by).click();

    // 12. Is 'Ön Bilgilendirme Formu Başarıyla kayd...' visible?
    by = By.xpath("//li[. = 'Ön Bilgilendirme Formu Başarıyla kaydedildi.']");
    driver.findElement(by).isDisplayed();

    // 13. Click 'Ödeme Ekranına Git'
    clickAction = JSActions.getClickAction();
    by = By.xpath("//a[. = '\n                \n                Ödeme Ekranına Git\n            ']");
    clickAction = (JSActions.ClickAction)((ReportingDriver)driver).addons().execute(clickAction, by, -1);

    // 14. Is 'ön bilgilendirme formu body' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//span[. = 'ön bilgilendirme formu body ']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 15. Click 'Ödeme Talimatı Oluştur Button'
    by = By.xpath("//input[@type='button' and @value='ÖDEME TALİMATI OLUŞTUR']");
    driver.findElement(by).click();

    // 16. Is 'ön bilgilendirme formu body formu oku...1' visible?
    GeneratedUtils.sleep(500);
    by = By.xpath("//li[. = 'ön bilgilendirme formu body  formu okumanız gerekmektedir.']");
    driver.findElement(by).isDisplayed();

    // 17. Click 'ön bilgilendirme formu body'
    GeneratedUtils.sleep(500);
    by = By.xpath("//span[. = 'ön bilgilendirme formu body ']");
    driver.findElement(by).click();

    // 18. Using Javascript to scroll to the element
    scrollToElementAction = JSActions.scrollToElementAction(0);
    by = By.xpath("//input[@type='button' and @value='ÖDEME TALİMATI OLUŞTUR']");
    scrollToElementAction = (JSActions.ScrollToElementAction)((ReportingDriver)driver).addons().execute(scrollToElementAction, by, -1);

    // 19. Is 'sliderOdemeTalimati' visible?
    GeneratedUtils.sleep(500);
    by = By.cssSelector("div[class='slimScrollBar']");
    driver.findElement(by).isDisplayed();

    // 20. Move mouse to 'sliderOdemeTalimati'
    by = By.cssSelector("div[class='slimScrollBar']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 21. Click and hold
    clickAndHold = WebExtensions.getClickAndHold();
    clickAndHold = (WebExtensions.ClickAndHold)((ReportingDriver)driver).addons().execute(clickAndHold);

    // 22. Move mouse to 'E-Tahsilat Sistemi'
    by = By.xpath("//a[text()='E-Tahsilat Sistemi']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 23. Release left mouse button
    /* Step is disabled
    releaseClick = WebExtensions.getReleaseClick();
    releaseClick = (WebExtensions.ReleaseClick)((ReportingDriver)driver).addons().execute(releaseClick);
    */
    // 24. Click 'Ödeme Talimatı Oluştur Button'
    by = By.xpath("//input[@type='button' and @value='ÖDEME TALİMATI OLUŞTUR']");
    driver.findElement(by).click();

    // 25. Is 'ön bilgilendirme formu body formu oku...1' not present or invisible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//li[. = 'ön bilgilendirme formu body  formu okumanız gerekmektedir.']");
    (new WebDriverWait(driver, 2)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 26. Move mouse to 'YÖNETİCİ1'
    by = By.xpath("//a[. = 'YÖNETİCİ']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 27. Click 'YÖNETİCİ1'
    clickAction = JSActions.getClickAction();
    by = By.xpath("//a[. = 'YÖNETİCİ']");
    clickAction = (JSActions.ClickAction)((ReportingDriver)driver).addons().execute(clickAction, by, -1);

    // 28. Move mouse to 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 29. Click 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    driver.findElement(by).click();

    // 30. Click 'btnParametreler'
    by = By.cssSelector("a[title='PARAMETRELER']");
    driver.findElement(by).click();

    // 31. Click 'li > a > Ön Bilgilendirme Formu'
    by = By.xpath("//a[normalize-space()='Ön Bilgilendirme Formu']");
    driver.findElement(by).click();

    // 32. Is 'PreInformationForm.ScrollRequired' visible?
    by = By.cssSelector("#PreInformationForm_ScrollRequired");
    driver.findElement(by).isDisplayed();

    // 33. Run Clear 'PreInformationForm.ScrollRequired' contents and type '0'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("0");
    by = By.cssSelector("#PreInformationForm_ScrollRequired");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 34. Is 'editorToolbar' visible?
    by = By.xpath("//div[contains(@id,'toolbargroup') and not(contains(@id,'Locales' ))]");
    driver.findElement(by).isDisplayed();

    // 35. Click 'Kaydet1'
    by = By.cssSelector("#kaydet");
    driver.findElement(by).click();

    // 36. Is 'Ön Bilgilendirme Formu Başarıyla kayd...' visible?
    by = By.xpath("//li[. = 'Ön Bilgilendirme Formu Başarıyla kaydedildi.']");
    driver.findElement(by).isDisplayed();

    // 37. Click 'Ödeme Ekranına Git'
    clickAction = JSActions.getClickAction();
    by = By.xpath("//a[. = '\n                \n                Ödeme Ekranına Git\n            ']");
    clickAction = (JSActions.ClickAction)((ReportingDriver)driver).addons().execute(clickAction, by, -1);

    // 38. Is 'ön bilgilendirme formu body' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//span[. = 'ön bilgilendirme formu body ']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 39. Click 'Ödeme Talimatı Oluştur Button'
    by = By.xpath("//input[@type='button' and @value='ÖDEME TALİMATI OLUŞTUR']");
    driver.findElement(by).click();

    // 40. Is 'ön bilgilendirme formu body formu oku...1' not present or invisible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//li[. = 'ön bilgilendirme formu body  formu okumanız gerekmektedir.']");
    (new WebDriverWait(driver, 2)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon.netahsilatdemo.com/Account/LogOff","otomasyon@test.com","Kd!cyhz4","","",""));
  }
}
