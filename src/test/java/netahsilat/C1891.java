package netahsilat;

import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C1891 Günlük Gelen Ödeme Toplamları Raporunda Çoklu Müşteri Temsilci Seçimi (NTH-14686)
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Mon Oct 10 08:34:34 GMT 2022.
 */
@DisplayName("C1891 Günlük Gelen Ödeme Toplamları Raporunda Çoklu Müşteri Temsilci Seçimi (NTH-14686)")
public class C1891 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C1891 Günlük Gelen Ödeme Toplamları Raporunda Çoklu Müşteri Temsilci Seçimi (NTH-14686)")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String password, String currentDateTime, String ElementCount)
      throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogin methodlogin;
    String string;

    // 1. Navigate to '{{Otomasyon2Logout}}'
    //    Navigates the specified URL (Auto-generated)
    driver.navigate().to(ProjectParameters.Otomasyon2Logout);

    // 2. Run Method Login
    //    admin@netahsilat.com
    methodlogin = new MethodLogin();
    methodlogin.ApplicationURL = ApplicationURL;
    methodlogin.userName = "admin@netahsilat.com";
    methodlogin.password = ProjectParameters.genelPassword;
    methodlogin.execute(driver);
    currentDateTime = String.valueOf(methodlogin.currentDateTime);

    // 3. Move mouse to 'moveToRaporlar'
    by = By.cssSelector("li[class='states']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 4. Click 'RAPORLAR'
    by = By.xpath("//a[. = 'RAPORLAR']");
    driver.findElement(by).click();

    // 5. Click 'GENEL RAPORLAR'
    by = By.xpath("//a[. = 'GENEL RAPORLAR']");
    driver.findElement(by).click();

    // 6. Click 'Günlük gelen ödeme toplamları'
    by = By.xpath("//a[contains(text(),'Günlük gelen ödeme toplamları')]/../a");
    driver.findElement(by).click();

    // 7. Is 'Günlük Gelen Ödeme Toplamları H1' visible?
    by = By.xpath("//h1[. = 'Günlük Gelen Ödeme Toplamları']");
    driver.findElement(by).isDisplayed();

    // 8. Click ' Müşteri Temsilcisi RadioButton'
    by = By.xpath("//input[@id='filteredByRepresentative']");
    driver.findElement(by).click();

    // 9. Type 'Site Müşteri Temsilcisi 1' in 'Textbox - Müşteri Temsilcisi'
    by = By.xpath("//ul[@class='select2-choices']/..//input");
    driver.findElement(by).sendKeys("Site Müşteri Temsilcisi 1");

    // 10. Click 'Site Müşteri Temsilcisi 1'
    by = By.xpath("//li[1]//span[. = 'Site Müşteri Temsilcisi 1']");
    driver.findElement(by).click();

    // 11. Type 'Site Müşteri Temsilcisi 2' in 'Textbox - Müşteri Temsilcisi'
    by = By.xpath("//ul[@class='select2-choices']/..//input");
    driver.findElement(by).sendKeys("Site Müşteri Temsilcisi 2");

    // 12. Click 'Site Müşteri Temsilcisi 2'
    by = By.xpath("//li[1]//span[. = 'Site Müşteri Temsilcisi 2']");
    driver.findElement(by).click();

    // 13. Click 'Rapor Oluştur'
    by = By.cssSelector("#createReport");
    driver.findElement(by).click();

    // 14. Is 'loading' not present or invisible?
    by = By.cssSelector("a[class='t-icon t-refresh t-loading']");
    (new WebDriverWait(driver, 15)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));

    // 15. Is ' Müşteri Temsilcisi RadioButton' visible and selected?
    by = By.xpath("//input[@id='filteredByRepresentative']");
    (new WebDriverWait(driver, 15)).until(ExpectedConditions.elementSelectionStateToBe(by, true));

    // 16. Is 'Site Müşteri Temsilcisi 1 TextboxArea' visible?
    by = By.xpath("//ul[@class='select2-choices']/.//li//div[text()='Site Müşteri Temsilcisi 1']");
    driver.findElement(by).isDisplayed();

    // 17. Is 'Site Müşteri Temsilcisi 2 TextboxArea' visible?
    by = By.xpath("//ul[@class='select2-choices']/.//li//div[text()='Site Müşteri Temsilcisi 2']");
    driver.findElement(by).isDisplayed();

    // 18. Get text from 'Ödeme Adedi'
    by = By.xpath("//tr[@class='odd'][2]/td[2]");
    string = driver.findElement(by).getAttribute("value");
    Assertions.assertTrue(Integer.parseInt(string) > Integer.parseInt("1"));

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon2.netahsilatdemo.com/","Kd!cyhz4","",""));
  }
}
