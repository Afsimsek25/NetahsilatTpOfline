package netahsilat;

import io.testproject.addon.WebExtensions;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C640 Müşteri Ekleme
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Thu Oct 06 12:34:10 GMT 2022.
 */
@DisplayName("C640 Müşteri Ekleme")
public class C640 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C640 Müşteri Ekleme")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String userName, String password) throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogoutlogin methodlogoutlogin;
    WebExtensions.SelectOptionbyVisibleText selectOptionbyVisibleText;

    // 1. Run Method Logout&Login
    GeneratedUtils.sleep(500);
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = ProjectParameters.genelEmail;
    methodlogoutlogin.password = ProjectParameters.genelPassword;
    methodlogoutlogin.execute(driver);

    // 2. Move mouse to 'moveUstUyeYonetimi'
    by = By.cssSelector("li[class='content']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 3. Click 'ustUyeYonetimi'
    by = By.xpath("//li[4]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();

    // 4. Click 'altUyeYonetimi'
    by = By.xpath("//li[1]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();

    // 5. Is 'ddYeniUyeTanimi' visible?
    by = By.xpath("//div[2]/div/img");
    driver.findElement(by).isDisplayed();

    // 6. Click 'ddYeniUyeTanimi'
    by = By.xpath("//div[2]/div/img");
    driver.findElement(by).click();

    // 7. Click 'Yeni Müşteri Tanımla'
    by = By.xpath("//a[. = 'Yeni Müşteri Tanımla']");
    driver.findElement(by).click();

    // 8. Type 'Özge' in 'Entity.VendorUser.Name'
    by = By.cssSelector("#Entity_VendorUser_Name");
    driver.findElement(by).sendKeys("Özge");

    // 9. Type 'Güngördü' in 'Entity.VendorUser.Surname'
    by = By.cssSelector("#Entity_VendorUser_Surname");
    driver.findElement(by).sendKeys("Güngördü");

    // 10. Type '123Ozge/' in 'Entity.EditPassword'
    by = By.cssSelector("#Entity_EditPassword");
    driver.findElement(by).sendKeys("123Ozge/");

    // 11. Type '123Ozge/' in 'Entity.EditRePassword'
    by = By.cssSelector("#Entity_EditRePassword");
    driver.findElement(by).sendKeys("123Ozge/");

    // 12. Type '12345' in 'Entity.Code'
    by = By.cssSelector("#Entity_Code");
    driver.findElement(by).sendKeys("12345");

    // 13. Select options in 'CurrencyTLBtn' with text '[NONE]'
    selectOptionbyVisibleText = WebExtensions.selectOptionbyVisibleText("TL");
    by = By.xpath("//select[@id='Entity_CurrencyTypeId']");
    selectOptionbyVisibleText = (WebExtensions.SelectOptionbyVisibleText)((ReportingDriver)driver).addons().execute(selectOptionbyVisibleText, by, -1);

    // 14. Select options in 'Language' with text '[NONE]'
    selectOptionbyVisibleText = WebExtensions.selectOptionbyVisibleText("Türkçe");
    by = By.cssSelector("#Entity_LanguageId");
    selectOptionbyVisibleText = (WebExtensions.SelectOptionbyVisibleText)((ReportingDriver)driver).addons().execute(selectOptionbyVisibleText, by, -1);

    // 15. Type '12345678' in 'Entity.ErpCode'
    by = By.cssSelector("#Entity_ErpCode");
    driver.findElement(by).sendKeys("12345678");

    // 16. Type 'quality@museum.com' in 'Entity.VendorUser.Email'
    by = By.cssSelector("#Entity_VendorUser_Email");
    driver.findElement(by).sendKeys("quality@museum.com");

    // 17. Select options in 'Entity.SelectedCountry' with text '[NONE]'
    selectOptionbyVisibleText = WebExtensions.selectOptionbyVisibleText("Turkey");
    by = By.cssSelector("#Entity_SelectedCountry");
    selectOptionbyVisibleText = (WebExtensions.SelectOptionbyVisibleText)((ReportingDriver)driver).addons().execute(selectOptionbyVisibleText, by, -1);

    // 18. Type '22185081940' in 'Entity.Tckn'
    by = By.cssSelector("#Entity_Tckn");
    driver.findElement(by).sendKeys("22185081940");

    // 19. Type 'otomasyon test' in 's2id_autogen2'
    by = By.cssSelector("#s2id_autogen2");
    driver.findElement(by).sendKeys("otomasyon test");

    // 20. Send 'ENTER' key(s)
    by = By.cssSelector("#s2id_autogen2");
    (new Actions(driver)).sendKeys(GeneratedUtils.getKeys("ENTER")).build().perform();

    // 21. Select options in 'Entity.PaymentSetDefinitionId' with text '[NONE]'
    selectOptionbyVisibleText = WebExtensions.selectOptionbyVisibleText("Furkan Test");
    by = By.cssSelector("#Entity_PaymentSetDefinitionId");
    selectOptionbyVisibleText = (WebExtensions.SelectOptionbyVisibleText)((ReportingDriver)driver).addons().execute(selectOptionbyVisibleText, by, -1);

    // 22. Click 'buttonKaydiTamamla'
    by = By.xpath("//button[. = 'Tamam']");
    driver.findElement(by).click();

    // 23. Is 'buttonKaydiTamamla' not present or invisible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//button[. = 'Tamam']");
    (new WebDriverWait(driver, 35)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon.netahsilatdemo.com/Account/LogOff","",""));
  }
}
