package netahsilat;

import io.testproject.addon.CombinedActions;
import io.testproject.addon.JSActions;
import io.testproject.addon.NetahsilatUtills;
import io.testproject.addon.VisibleElementsOperations;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C755 Kullanıcı şifre kuralları
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Fri Oct 07 11:07:57 GMT 2022.
 */
@DisplayName("C755 Kullanıcı şifre kuralları")
public class C755 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C755 Kullanıcı şifre kuralları")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String userName, String password) throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogoutlogin methodlogoutlogin;
    CombinedActions.ClearAndTypeAction clearAndTypeAction;
    NetahsilatUtills.ClickUntilGone clickUntilGone;
    JSActions.ClickAction clickAction;
    VisibleElementsOperations.ClickIfVisibleWeb clickIfVisibleWeb;

    /// 1. Run Method Logout&Login
    GeneratedUtils.sleep(500);
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = ProjectParameters.genelEmail;
    methodlogoutlogin.password = ProjectParameters.genelPassword;
    methodlogoutlogin.execute(driver);

    // 2. Move mouse to 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 3. Click 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    driver.findElement(by).click();

    // 4. Click 'btnParametreler'
    by = By.cssSelector("a[title='PARAMETRELER']");
    driver.findElement(by).click();

    // 5. Is 'li > a > Kullanıcı Şifre Kuralları' visible?
    by = By.xpath("//a[normalize-space()='Kullanıcı Şifre Kuralları']");
    driver.findElement(by).isDisplayed();

    // 6. Click 'li > a > Kullanıcı Şifre Kuralları'
    by = By.xpath("//a[normalize-space()='Kullanıcı Şifre Kuralları']");
    driver.findElement(by).click();

    // 7. Is 'label - Şifre kuralları kullanılsın mı?' visible?
    by = By.xpath("//label[. = 'Şifre kuralları kullanılsın mı?']");
    driver.findElement(by).isDisplayed();

    // 8. Click 'Şifre kuralları kullanılsın mı? (Checked)' if it's visible
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("0");
    by = By.xpath("//label[normalize-space()='Şifre kuralları kullanılsın mı?']/../..//..//span[@class='jquery-safari-checkbox jquery-safari-checkbox-checked']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);

    // 9. Click 'Kaydet1'
    by = By.cssSelector("#kaydet");
    driver.findElement(by).click();

    // 10. Is 'Kullanıcı Şifre Kuralları Başarıyla k...' visible?
    by = By.xpath("//li[. = 'Kullanıcı Şifre Kuralları Başarıyla kaydedildi.']");
    driver.findElement(by).isDisplayed();

    // 11. Click 'altUyeYonetimi'
    clickAction = JSActions.getClickAction();
    by = By.xpath("//li[1]/a[. = 'ÜYE YÖNETİMİ']");
    clickAction = (JSActions.ClickAction)((ReportingDriver)driver).addons().execute(clickAction, by, -1);

    // 12. Is 'SearchVendorName' visible?
    by = By.cssSelector("#SearchVendorName");
    driver.findElement(by).isDisplayed();

    // 13. Type '11905615626' in 'SearchVendorName'
    by = By.cssSelector("#SearchVendorName");
    driver.findElement(by).sendKeys("11905615626");

    // 14. Click '11905615626'
    by = By.xpath("//div[contains(text(),'11905615626')]");
    driver.findElement(by).click();

    // 15. Is 'Refresh' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.cssSelector("a[class='t-icon t-refresh']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 16. Click 'btnListele'
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();

    // 17. Is 'Refresh Loading' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//a[@class='t-icon t-refresh t-loading']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 18. Is 'Refresh noLoading' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//a[@class='t-icon t-refresh']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 19. Click 'C755 Müşteri / Düzenle'
    by = By.xpath("//tr[1]/td[contains(text(),'c755@musteri.com')]/..//img[contains(@src, 'edit.png')]");
    driver.findElement(by).click();

    // 20. Type '1234' in 'Entity.EditPassword'
    by = By.cssSelector("#Entity_EditPassword");
    driver.findElement(by).sendKeys("1234");

    // 21. Type '1234' in 'Entity.EditRePassword'
    by = By.cssSelector("#Entity_EditRePassword");
    driver.findElement(by).sendKeys("1234");

    // 22. Run Click Until Gone
    clickUntilGone = NetahsilatUtills.clickUntilGone(30);
    by = By.xpath("//button[. = 'Tamam']");
    clickUntilGone = (NetahsilatUtills.ClickUntilGone)((ReportingDriver)driver).addons().execute(clickUntilGone, by, -1);

    // 23. Move mouse to 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 24. Click 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    driver.findElement(by).click();

    // 25. Click 'btnParametreler'
    by = By.cssSelector("a[title='PARAMETRELER']");
    driver.findElement(by).click();

    // 26. Is 'li > a > Kullanıcı Şifre Kuralları' visible?
    by = By.xpath("//a[normalize-space()='Kullanıcı Şifre Kuralları']");
    driver.findElement(by).isDisplayed();

    // 27. Click 'li > a > Kullanıcı Şifre Kuralları'
    by = By.xpath("//a[normalize-space()='Kullanıcı Şifre Kuralları']");
    driver.findElement(by).click();

    // 28. Is 'label - Şifre kuralları kullanılsın mı?' visible?
    by = By.xpath("//label[. = 'Şifre kuralları kullanılsın mı?']");
    driver.findElement(by).isDisplayed();

    // 29. Click ' Şifre kuralları kullanılsın mı? (Unchecked)'
    by = By.xpath("//label[normalize-space()='Şifre kuralları kullanılsın mı?']/../..//..//span[@class='jquery-safari-checkbox']");
    driver.findElement(by).click();

    // 30. Run Clear 'PasswordRuleSetting.MinLowerCaseLette...' contents and type '2'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("2");
    by = By.cssSelector("#PasswordRuleSetting_MinLowerCaseLetterLength");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 31. Run Clear 'PasswordRuleSetting.MinNumberCharacte...' contents and type '2'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("2");
    by = By.cssSelector("#PasswordRuleSetting_MinNumberCharacterLength");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 32. Run Clear 'PasswordRuleSetting.MinRequiredPasswo...' contents and type '10'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("10");
    by = By.cssSelector("#PasswordRuleSetting_MinRequiredPasswordLength");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 33. Run Clear 'PasswordRuleSetting.MinSpecialCharact...' contents and type '2'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("2");
    by = By.cssSelector("#PasswordRuleSetting_MinSpecialCharacterLength");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 34. Run Clear 'PasswordRuleSetting.MinUpperCaseLette...' contents and type '2'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("2");
    by = By.cssSelector("#PasswordRuleSetting_MinUpperCaseLetterLength");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 35. Click 'Kaydet1'
    by = By.cssSelector("#kaydet");
    driver.findElement(by).click();

    // 36. Is 'Kullanıcı Şifre Kuralları Başarıyla k...' visible?
    by = By.xpath("//li[. = 'Kullanıcı Şifre Kuralları Başarıyla kaydedildi.']");
    driver.findElement(by).isDisplayed();

    // 37. Move mouse to 'moveUstUyeYonetimi'
    by = By.cssSelector("li[class='content']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 38. Click 'moveUstUyeYonetimi'
    by = By.cssSelector("li[class='content']");
    driver.findElement(by).click();

    // 39. Click 'altUyeYonetimi'
    by = By.xpath("//li[1]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();

    // 40. Is 'SearchVendorName' visible?
    by = By.cssSelector("#SearchVendorName");
    driver.findElement(by).isDisplayed();

    // 41. Type '11905615626' in 'SearchVendorName'
    by = By.cssSelector("#SearchVendorName");
    driver.findElement(by).sendKeys("11905615626");

    // 42. Click '11905615626'
    by = By.xpath("//div[contains(text(),'11905615626')]");
    driver.findElement(by).click();

    // 43. Click 'btnListele'
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();

    // 44. Is 'Refresh Loading' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//a[@class='t-icon t-refresh t-loading']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 45. Is 'Refresh noLoading' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//a[@class='t-icon t-refresh']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 46. Click 'C755 Müşteri / Düzenle' if it's visible
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(25000, TimeUnit.MILLISECONDS);
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("");
    by = By.xpath("//tr[1]/td[contains(text(),'c755@musteri.com')]/..//img[contains(@src, 'edit.png')]");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 47. Type 'Aaa11**000' in 'Entity.EditPassword'
    by = By.cssSelector("#Entity_EditPassword");
    driver.findElement(by).sendKeys("Aaa11**000");

    // 48. Type 'Aaa11**000' in 'Entity.EditRePassword'
    by = By.cssSelector("#Entity_EditRePassword");
    driver.findElement(by).sendKeys("Aaa11**000");

    // 49. Click 'Tamam1'
    by = By.xpath("//button[. = 'Tamam']");
    driver.findElement(by).click();

    // 50. Does 'Şifrenizde,minimum 2 büyük harf, kull...' contain 'Şifrenizde,minimum 2 büyük harf, kullanmalısınız'?
    by = By.xpath("//li[. = 'Şifrenizde,minimum 2 büyük harf, kullanmalısınız']");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Şifrenizde,minimum 2 büyük harf, kullanmalısınız"));

    // 51. Click 'btnKapat'
    by = By.xpath("//button[. = 'Kapat']");
    driver.findElement(by).click();

    // 52. Run Clear 'Entity.EditPassword' contents and type 'AAa11**000'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("AAa11**000");
    by = By.cssSelector("#Entity_EditPassword");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 53. Run Clear 'Entity.EditRePassword' contents and type 'AAa11**000'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("AAa11**000");
    by = By.cssSelector("#Entity_EditRePassword");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 54. Click 'Tamam1'
    by = By.xpath("//button[. = 'Tamam']");
    driver.findElement(by).click();

    // 55. Does 'Şifrenizde,minimum 2 küçük harf, kull...' contain 'Şifrenizde,minimum 2 küçük harf, kullanmalısınız'?
    by = By.xpath("//li[. = 'Şifrenizde,minimum 2 küçük harf, kullanmalısınız']");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Şifrenizde,minimum 2 küçük harf, kullanmalısınız"));

    // 56. Click 'btnKapat'
    by = By.xpath("//button[. = 'Kapat']");
    driver.findElement(by).click();

    // 57. Run Clear 'Entity.EditPassword' contents and type 'AAaa1**aaa'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("AAaa1**aaa");
    by = By.cssSelector("#Entity_EditPassword");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 58. Run Clear 'Entity.EditRePassword' contents and type 'AAaa1**aaa'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("AAaa1**aaa");
    by = By.cssSelector("#Entity_EditRePassword");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 59. Click 'Tamam1'
    by = By.xpath("//button[. = 'Tamam']");
    driver.findElement(by).click();

    // 60. Does 'Şifrenizde,minimum 2 rakam, kullanmal...' contain 'Şifrenizde,minimum 2 rakam, kullanmalısınız'?
    by = By.xpath("//li[. = 'Şifrenizde,minimum 2 rakam, kullanmalısınız']");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Şifrenizde,minimum 2 rakam, kullanmalısınız"));

    // 61. Click 'btnKapat'
    by = By.xpath("//button[. = 'Kapat']");
    driver.findElement(by).click();

    // 62. Run Clear 'Entity.EditPassword' contents and type 'AAaa11*aaa'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("AAaa11*aaa");
    by = By.cssSelector("#Entity_EditPassword");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 63. Run Clear 'Entity.EditRePassword' contents and type 'AAaa11*aaa'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("AAaa11*aaa");
    by = By.cssSelector("#Entity_EditRePassword");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 64. Click 'Tamam1'
    by = By.xpath("//button[. = 'Tamam']");
    driver.findElement(by).click();

    // 65. Does 'Şifrenizde,minimum 2 özel karakter, k...' contain 'Şifrenizde,minimum 2 özel karakter, kullanmalısınız'?
    by = By.xpath("//li[. = 'Şifrenizde,minimum 2 özel karakter, kullanmalısınız']");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Şifrenizde,minimum 2 özel karakter, kullanmalısınız"));

    // 66. Click 'btnKapat'
    by = By.xpath("//button[. = 'Kapat']");
    driver.findElement(by).click();

    // 67. Run Clear 'Entity.EditPassword' contents and type 'AAaa11**'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("AAaa11**");
    by = By.cssSelector("#Entity_EditPassword");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 68. Run Clear 'Entity.EditRePassword' contents and type 'AAaa11**'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("AAaa11**");
    by = By.cssSelector("#Entity_EditRePassword");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 69. Click 'Tamam1'
    by = By.xpath("//button[. = 'Tamam']");
    driver.findElement(by).click();

    // 70. Does 'Şifrenizde,minimum 10 karakter uzunlu...' contain 'Şifrenizde,minimum 10 karakter uzunluk, kullanmalısınız'?
    by = By.xpath("//li[. = 'Şifrenizde,minimum 10 karakter uzunluk, kullanmalısınız']");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Şifrenizde,minimum 10 karakter uzunluk, kullanmalısınız"));

    // 71. Click 'btnKapat'
    by = By.xpath("//button[. = 'Kapat']");
    driver.findElement(by).click();

    // 72. Run Clear 'Entity.EditPassword' contents and type 'AAaa11**aa'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("AAaa11**aa");
    by = By.cssSelector("#Entity_EditPassword");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 73. Run Clear 'Entity.EditRePassword' contents and type 'AAaa11**aa'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("AAaa11**aa");
    by = By.cssSelector("#Entity_EditRePassword");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 74. Run Click Until Gone
    clickUntilGone = NetahsilatUtills.clickUntilGone(30);
    by = By.xpath("//button[. = 'Tamam']");
    clickUntilGone = (NetahsilatUtills.ClickUntilGone)((ReportingDriver)driver).addons().execute(clickUntilGone, by, -1);

    // 75. Run Method Logout&Login
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = "c755@musteri.com";
    methodlogoutlogin.password = "AAaa11**aa";
    methodlogoutlogin.execute(driver);

    // 76. Is 'PureAmount' visible?
    by = By.cssSelector("#PureAmount");
    driver.findElement(by).isDisplayed();

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon.netahsilatdemo.com/Account/LogOff","otomasyon@test.com","Kd!cyhz4"));
  }
}
