package netahsilat;

import io.testproject.addon.CombinedActions;
import io.testproject.addon.GridOperations;
import io.testproject.addon.NetahsilatUtills;
import io.testproject.addon.WebExtensions;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import java.lang.Exception;
import java.lang.Override;
import java.lang.RuntimeException;
import java.lang.String;
import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C1689 Rol Grubu Ekleme/Güncelleme/Sil (Site)
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Mon Oct 10 07:12:19 GMT 2022.
 */
@DisplayName("C1689 Rol Grubu Ekleme/Güncelleme/Sil (Site)")
public class C1689 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C1689 Rol Grubu Ekleme/Güncelleme/Sil (Site)")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String userName, String password, String currentDateTime)
      throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogin methodlogin;
    CombinedActions.ClearAndTypeAction clearAndTypeAction;
    WebExtensions.IsEnabled isEnabled;
    NetahsilatUtills.ClickUntilGone clickUntilGone;
    GridOperations.conditionalClick conditionalClick;

    // 1. Navigate to '{{Otomasyon3Logout}}'
    driver.navigate().to(ProjectParameters.Otomasyon3Logout);

    // 2. Run Method Login
    methodlogin = new MethodLogin();
    methodlogin.ApplicationURL = ApplicationURL;
    methodlogin.userName = ProjectParameters.genelEmail;
    methodlogin.password = ProjectParameters.genelPassword;
    methodlogin.execute(driver);
    currentDateTime = String.valueOf(methodlogin.currentDateTime);

    // 3. Move mouse to 'moveToKullaniciYönetimi'
    by = By.cssSelector("li[class='user']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 4. Click 'KULLANICI YÖNETİMİ1'
    by = By.cssSelector("a[title='KULLANICI YÖNETİMİ']");
    driver.findElement(by).click();

    // 5. Click 'ROL YÖNETİMİ'
    by = By.xpath("//a[. = 'ROL YÖNETİMİ']");
    driver.findElement(by).click();

    // 6. Move mouse to 'Yeni Rol Grubu Tanımla DropdownMenu'
    by = By.xpath("//h1[text()='Yeni Rol Grubu Tanımla']/../div");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 7. Click 'Site Yetkisi'
    by = By.xpath("//li[@class='icon_bayi']/.//a[text()='Site Yetkisi']");
    driver.findElement(by).click();

    // 8. Type 'OtomasyonSiteAdmin' in 'Rol Grubu Adı'
    by = By.xpath("//input[@id='RoleGroupName']");
    driver.findElement(by).sendKeys("OtomasyonSiteAdmin");

    // 9. Does 'Yetki Tipi' contain 'Site'?
    by = By.cssSelector("#RoleGroupType");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Site"));

    // 10. Run Is Enabled?
    isEnabled = WebExtensions.getIsEnabled();
    by = By.cssSelector("#RoleGroupType");
    isEnabled = (WebExtensions.IsEnabled)((ReportingDriver)driver).addons().execute(isEnabled, by, -1);

    // 11. Click 'Yeni Rol Ekle Yönetici Checkbox'
    by = By.cssSelector("#chckIsAdmin");
    driver.findElement(by).click();

    // 12. Is 'Yeni Rol Ekle Yönetici Checkbox' visible and selected?
    by = By.cssSelector("#chckIsAdmin");
    (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));

    // 13. Click 'Tamam'
    by = By.xpath("//button[. = 'Tamam']");
    driver.findElement(by).click();

    // 14. Is 'Refresh' visible?
    by = By.xpath("//a[. = 'Refresh']");
    driver.findElement(by).isDisplayed();

    // 15. Run Conditional Click
    conditionalClick = GridOperations.conditionalClick("//td[text()='OtomasyonSiteAdmin']",false);
    by = By.xpath("//span[. = 'next']");
    conditionalClick = (GridOperations.conditionalClick)((ReportingDriver)driver).addons().execute(conditionalClick, by, -1);

    // 16. Click 'OtomasyonSiteAdmin Düzenle'
    by = By.xpath("//td[text()='OtomasyonSiteAdmin']/..//img[contains(@src,'edit.png')]");
    driver.findElement(by).click();

    // 17. Run Clear '//input[@id='RoleGroupName'][@value='OtomasyonSiteAdmin']' contents and type 'OtomasyonSiteAdminGüncelle'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("OtomasyonSiteAdminGüncelle");
    by = By.xpath("//input[@id='RoleGroupName'][@value='OtomasyonSiteAdmin']");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 18. Click 'Yeni Rol Ekle Yönetici Checkbox'
    by = By.cssSelector("#chckIsAdmin");
    driver.findElement(by).click();

    // 19. Is 'Yeni Rol Ekle Yönetici Checkbox' visible and selected?
    //    INVERT STEP RESULT

    try {
      by = By.cssSelector("#chckIsAdmin");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is 'Yeni Rol Ekle Yönetici Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }

    // 20. Click 'Cari Hesap Hareket Yönetimi Checkbox'
    by = By.xpath("//span[text()='Cari Hesap Hareket Yönetimi']//..//input[2]");
    driver.findElement(by).click();

    // 21. Is 'Cari Hesap Hareket Yönetimi Checkbox' visible and selected?
    //    INVERT STEP RESULT

    try {
      by = By.xpath("//span[text()='Cari Hesap Hareket Yönetimi']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is 'Cari Hesap Hareket Yönetimi Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }

    // 22. Click ' Ödeme Seti Checkbox'
    by = By.xpath("//span[text()='Ödeme Seti']//..//input[2]");
    driver.findElement(by).click();

    // 23. Is ' Ödeme Seti Checkbox' visible and selected?
    //    INVERT STEP RESULT

    try {
      by = By.xpath("//span[text()='Ödeme Seti']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is ' Ödeme Seti Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }

    // 24. Click ' Ödeme Seti Grup Yönetimi Checkbox1'
    by = By.xpath("//span[text()='Ödeme Seti Grup Yönetimi']//..//input[2]");
    driver.findElement(by).click();

    // 25. Is ' Ödeme Seti Grup Yönetimi Checkbox1' visible and selected?
    //    INVERT STEP RESULT

    try {
      by = By.xpath("//span[text()='Ödeme Seti Grup Yönetimi']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is ' Ödeme Seti Grup Yönetimi Checkbox1' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }

    // 26. Click 'Site Yönetimi Checkvbox'
    by = By.xpath("//span[text()='Site Yönetimi']//..//input[2]");
    driver.findElement(by).click();

    // 27. Is 'Site Yönetimi Checkvbox' visible and selected?
    //    INVERT STEP RESULT

    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    try {
      by = By.xpath("//span[text()='Site Yönetimi']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is 'Site Yönetimi Checkvbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 28. Click 'Tahsilat Yöntemleri + işareti'
    by = By.xpath("//span[text()='Tahsilat Yöntemleri']//..//span[@class='t-icon t-plus']");
    driver.findElement(by).click();

    // 29. Click 'Email Order Takip Checkbox'
    by = By.xpath("//span[text()='EMail Order Takip']//..//input[2]");
    driver.findElement(by).click();

    // 30. Is 'Email Order Takip Checkbox' visible and selected?
    //    INVERT STEP RESULT

    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    try {
      by = By.xpath("//span[text()='EMail Order Takip']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is 'Email Order Takip Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 31. Click 'Email Order Yönetim Checkbox'
    by = By.xpath("//span[text()='Email Order Yönetim']//..//input[2]");
    driver.findElement(by).click();

    // 32. Is 'Email Order Yönetim Checkbox' visible and selected?
    //    INVERT STEP RESULT

    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    try {
      by = By.xpath("//span[text()='Email Order Yönetim']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is 'Email Order Yönetim Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 33. Click 'Email Order Checkbox'
    by = By.xpath("//span[text()='Email Order']//..//input[2]");
    driver.findElement(by).click();

    // 34. Is 'Email Order Checkbox' visible and selected?
    //    INVERT STEP RESULT

    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    try {
      by = By.xpath("//span[text()='Email Order']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is 'Email Order Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 35. Click 'Toplu Email Order Checkbox'
    by = By.xpath("//span[text()='Toplu Email Order']//..//input[2]");
    driver.findElement(by).click();

    // 36. Is 'Toplu Email Order Checkbox' visible and selected?
    //    INVERT STEP RESULT

    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    try {
      by = By.xpath("//span[text()='Toplu Email Order']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is 'Toplu Email Order Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 37. Click 'Email Order (Üyeliksiz) Checkbox'
    by = By.xpath("//span[text()='Email Order (Üyeliksiz)']//..//input[2]");
    driver.findElement(by).click();

    // 38. Is 'Email Order (Üyeliksiz) Checkbox' visible and selected?
    //    INVERT STEP RESULT

    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    try {
      by = By.xpath("//span[text()='Email Order (Üyeliksiz)']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is 'Email Order (Üyeliksiz) Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 39. Click 'Toplu Email Order (Üyeliksiz) Checkbox'
    by = By.xpath("//span[text()='Toplu Email Order (Üyeliksiz)']//..//input[2]");
    driver.findElement(by).click();

    // 40. Is 'Toplu Email Order (Üyeliksiz) Checkbox' visible and selected?
    //    INVERT STEP RESULT

    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    try {
      by = By.xpath("//span[text()='Toplu Email Order (Üyeliksiz)']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is 'Toplu Email Order (Üyeliksiz) Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 41. Click 'Tamam'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    by = By.xpath("//button[. = 'Tamam']");
    driver.findElement(by).click();
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 42. Is 'Refresh' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    by = By.xpath("//a[. = 'Refresh']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 43. Click 'OtomasyonSiteAdminGüncelle Düzenle'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    by = By.xpath("//td[text()='OtomasyonSiteAdminGüncelle']/..//img[contains(@src,'edit.png')]");
    driver.findElement(by).click();
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 44. Does 'Yetki Tipi' contain 'Site'?
    by = By.cssSelector("#RoleGroupType");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Site"));

    // 45. Run Is Enabled?
    isEnabled = WebExtensions.getIsEnabled();
    by = By.cssSelector("#RoleGroupType");
    isEnabled = (WebExtensions.IsEnabled)((ReportingDriver)driver).addons().execute(isEnabled, by, -1);

    // 46. Is 'Yeni Rol Ekle Yönetici Checkbox' visible and selected?
    //    INVERT STEP RESULT

    try {
      by = By.cssSelector("#chckIsAdmin");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is 'Yeni Rol Ekle Yönetici Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }

    // 47. Is 'Cari Hesap Hareket Yönetimi Checkbox' visible and selected?
    //    INVERT STEP RESULT

    try {
      by = By.xpath("//span[text()='Cari Hesap Hareket Yönetimi']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is 'Cari Hesap Hareket Yönetimi Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }

    // 48. Click 'Ödeme Seti + işareti'
    by = By.xpath("//span[text()='Ödeme Seti']//..//span[@class='t-icon t-plus']");
    driver.findElement(by).click();

    // 49. Is ' Ödeme Seti Checkbox' visible and selected?
    //    INVERT STEP RESULT

    try {
      by = By.xpath("//span[text()='Ödeme Seti']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is ' Ödeme Seti Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }

    // 50. Is ' Ödeme Seti Detayları Checkbox' visible and selected?
    //    INVERT STEP RESULT

    try {
      by = By.xpath("//span[text()='Ödeme Seti Detayları']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is ' Ödeme Seti Detayları Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }

    // 51. Is ' Ödeme Seti Yönetimi Checkbox' visible and selected?
    //    INVERT STEP RESULT

    try {
      by = By.xpath("//span[text()='Ödeme Seti Yönetimi']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is ' Ödeme Seti Yönetimi Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }

    // 52. Is ' Ödeme Seti Grup Yönetimi Checkbox' visible and selected?
    //    INVERT STEP RESULT

    try {
      by = By.xpath("//span[text()='Ödeme Seti Grup Yönetimi']//..//input[2]");
      (new WebDriverWait(driver, 2)).until(ExpectedConditions.elementSelectionStateToBe(by, true));
      Assertions.fail("Step 'Is ' Ödeme Seti Grup Yönetimi Checkbox' visible and selected?' result inverted, step failed");
    }
    catch (RuntimeException ex) {
    }

    // 53. Click 'Tamam'
    by = By.xpath("//button[. = 'Tamam']");
    driver.findElement(by).click();

    // 54. Click 'OtomasyonSiteAdminGüncelle Sil img'
    by = By.xpath("//td[text()='OtomasyonSiteAdminGüncelle']/..//img[contains(@src,'trash.png')]");
    driver.findElement(by).click();

    // 55. Run Click Until Gone
    clickUntilGone = NetahsilatUtills.clickUntilGone(30);
    by = By.xpath("//button[. = 'Tamam']");
    clickUntilGone = (NetahsilatUtills.ClickUntilGone)((ReportingDriver)driver).addons().execute(clickUntilGone, by, -1);

    // 56. Is 'OtomasyonSiteAdminGüncelle' not present or invisible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//td[text()='OtomasyonSiteAdminGüncelle']");
    (new WebDriverWait(driver, 2)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon3.netahsilatdemo.com/","otomasyon@test.com","Kd!cyhz4",""));
  }
}
