package netahsilat;

import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C1659 Son ödeme tarihi ve işlem tarihine göre filtreleme yapabilme
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Fri Oct 07 11:05:36 GMT 2022.
 */
@DisplayName("C1659 Son ödeme tarihi ve işlem tarihine göre filtreleme yapabilme")
public class C1659 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C1659 Son ödeme tarihi ve işlem tarihine göre filtreleme yapabilme")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String userName, String password, String currentDate,
      String faturaErpKodu, String faturaNumarasi, String dokumanKodu, String odemeLinki) throws
      Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogoutlogin methodlogoutlogin;
    String string;

    // 1. Run Method Logout&Login
    GeneratedUtils.sleep(500);
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = ProjectParameters.genelEmail;
    methodlogoutlogin.password = ProjectParameters.genelPassword;
    methodlogoutlogin.execute(driver);

    // 2. Move mouse to 'moveCariHesapYonetimi'
    GeneratedUtils.sleep(500);
    by = By.cssSelector(".accountmanagement");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 3. Click 'moveCariHesapYonetimi'
    GeneratedUtils.sleep(500);
    by = By.cssSelector(".accountmanagement");
    driver.findElement(by).click();

    // 4. Click 'CARİ HESAP HAREKETLERİ'
    GeneratedUtils.sleep(500);
    by = By.xpath("//a[. = 'CARİ HESAP HAREKETLERİ']");
    driver.findElement(by).click();

    // 5. Move mouse to 'FilterDueDateStart'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#FilterDueDateStart");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 6. Clear 'FilterDueDateStart' contents
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#FilterDueDateStart");
    driver.findElement(by).clear();

    // 7. Clear 'FilterDueDateEnd' contents
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#FilterDueDateEnd");
    driver.findElement(by).clear();

    // 8. Clear 'FilterTransactionDateStart' contents
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#FilterTransactionDateStart");
    driver.findElement(by).clear();

    // 9. Clear 'FilterTransactionDateEnd' contents
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#FilterTransactionDateEnd");
    driver.findElement(by).clear();

    // 10. Is 'Refresh' visible?
    GeneratedUtils.sleep(500);
    by = By.cssSelector("a[class='t-icon t-refresh']");
    driver.findElement(by).isDisplayed();

    // 11. Click 'btnListele'
    GeneratedUtils.sleep(500);
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();

    // 12. Is 'Refresh' visible?
    GeneratedUtils.sleep(500);
    by = By.cssSelector("a[class='t-icon t-refresh']");
    driver.findElement(by).isDisplayed();

    // 13. Get text from 'Displaying items'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    by = By.xpath("//div[contains(text(),'Displaying')]");
    string = driver.findElement(by).getAttribute("value");
    Assertions.assertTrue(string.startsWith("Displaying items 1 - 50 of"));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 14. Type '1.07.2021 16:52' in 'FilterTransactionDateStart'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#FilterTransactionDateStart");
    driver.findElement(by).sendKeys("1.07.2021 16:52");

    // 15. Type '1.07.2021 16:52' in 'FilterTransactionDateEnd'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#FilterTransactionDateEnd");
    driver.findElement(by).sendKeys("1.07.2021 16:52");

    // 16. Click 'btnListele'
    GeneratedUtils.sleep(500);
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();

    // 17. Does 'columnIslemDurumu' contain '01.07.21 16:52'?
    GeneratedUtils.sleep(500);
    by = By.xpath("//div[2]//td[2]");
    Assertions.assertTrue(driver.findElement(by).getText().contains("01.07.21 16:52"));

    // 18. Clear 'FilterTransactionDateStart' contents
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#FilterTransactionDateStart");
    driver.findElement(by).clear();

    // 19. Clear 'FilterTransactionDateEnd' contents
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#FilterTransactionDateEnd");
    driver.findElement(by).clear();

    // 20. Type '24.10.2029 00:00' in 'FilterDueDateStart'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#FilterDueDateStart");
    driver.findElement(by).sendKeys("24.10.2029 00:00");

    // 21. Type '24.10.2029 00:00' in 'FilterDueDateEnd'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#FilterDueDateEnd");
    driver.findElement(by).sendKeys("24.10.2029 00:00");

    // 22. Click 'btnListele'
    GeneratedUtils.sleep(500);
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();

    // 23. Get text from 'OtoTestERP555'
    //    Bu  Case Patlarsa CurrentAccountTransactions bu taploda 3376 id li kayıt deleted=false yapılacak.
    GeneratedUtils.sleep(500);
    by = By.xpath("//tr[1]//td[. = 'OtoTestERP555']");
    string = driver.findElement(by).getAttribute("value");
    Assertions.assertEquals("OtoTestERP555", string);

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon.netahsilatdemo.com/Account/LogOff","otomasyon@test.com","Kd!cyhz4","","","","",""));
  }
}
