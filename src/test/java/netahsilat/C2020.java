package netahsilat;

import io.testproject.addon.CombinedActions;
import io.testproject.addon.ImapMailOperations;
import io.testproject.addon.WebExtensions;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C2020 Email Order Tanımları - En Çok Hata Sayısı Kontrolü
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Fri Oct 07 11:24:12 GMT 2022.
 */
@DisplayName("C2020 Email Order Tanımları - En Çok Hata Sayısı Kontrolü")
public class C2020 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C2020 Email Order Tanımları - En Çok Hata Sayısı Kontrolü")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String userName, String password, String mail, String url,
      String referansno) throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogoutlogin methodlogoutlogin;
    MethodParametrikdemesadeceKartBilgisi methodparametrikdemesadecekartbilgisi;
    String string;
    WebExtensions.SelectOptionbyVisibleText selectOptionbyVisibleText;
    ImapMailOperations.ReadSpecificsMail readSpecificsMail;
    CombinedActions.ClearAndTypeAction clearAndTypeAction;
    ImapMailOperations.DeleteAllMails deleteAllMails;

    // 1. Run Method Logout&Login
    GeneratedUtils.sleep(500);
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = ProjectParameters.genelEmail;
    methodlogoutlogin.password = ProjectParameters.genelPassword;
    methodlogoutlogin.execute(driver);

    // 2. Delete All Mails
    deleteAllMails = ImapMailOperations.deleteAllMails("netahsilatotomasyon@yandex.com","cishmttwpcryalzi");
    deleteAllMails = (ImapMailOperations.DeleteAllMails)((ReportingDriver)driver).addons().execute(deleteAllMails);

    // 3. Move mouse to 'moveUstUyeYonetimi'
    by = By.cssSelector("li[class='content']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 4. Click 'moveUstUyeYonetimi'
    by = By.cssSelector("li[class='content']");
    driver.findElement(by).click();

    // 5. Click 'ustUyeYonetimi'
    by = By.xpath("//li[4]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();

    // 6. Move mouse to 'dropDownOdemeLinki'
    by = By.xpath("//li[2]/div/img");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 7. Click 'ÜYELİKSİZ ÖDEME LİNKİ'
    by = By.xpath("//a[. = 'ÜYELİKSİZ ÖDEME LİNKİ']");
    driver.findElement(by).click();

    // 8. Select options in 'PaymentSetId' with text '[NONE]'
    selectOptionbyVisibleText = WebExtensions.selectOptionbyVisibleText("Varsayılan ödeme seti");
    by = By.cssSelector("#PaymentSetId");
    selectOptionbyVisibleText = (WebExtensions.SelectOptionbyVisibleText)((ReportingDriver)driver).addons().execute(selectOptionbyVisibleText, by, -1);

    // 9. Run Clear 'PureAmount' contents and type '146,37'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("146,37");
    by = By.cssSelector("#PureAmount");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 10. Click 'Mail Order Devam Et'
    by = By.cssSelector("input[value='Devam Et']");
    driver.findElement(by).click();

    // 11. Type 'qa' in 'inputUnvan'
    by = By.cssSelector("#DisplayName");
    driver.findElement(by).sendKeys("qa");

    // 12. Type 'otomasyon' in 'FullName'
    by = By.cssSelector("#FullName");
    driver.findElement(by).sendKeys("otomasyon");

    // 13. Type 'netahsilatotomasyon@yandex.com' in 'EMail'
    by = By.cssSelector("#EMail");
    driver.findElement(by).sendKeys("netahsilatotomasyon@yandex.com");

    // 14. Type '5555555555' in 'Telefon Numarası'
    by = By.cssSelector("#Phone");
    driver.findElement(by).sendKeys("5555555555");

    // 15. Type '37653746593765' in 'Description'
    by = By.cssSelector("#Description");
    driver.findElement(by).sendKeys("37653746593765");

    // 16. Click 'Ekle3'
    by = By.xpath("//button[. = 'Ekle']");
    driver.findElement(by).click();

    // 17. Is 'netahsilatotomasyon@yandex.com1' visible?
    by = By.xpath("//td[. = 'netahsilatotomasyon@yandex.com']");
    driver.findElement(by).isDisplayed();

    // 18. Click 'UnvanEkleCheckbox'
    by = By.xpath("//td[text()='netahsilatotomasyon@yandex.com']//../td/input[@type='checkbox']");
    driver.findElement(by).click();

    // 19. Click 'emailGonder'
    by = By.xpath("//div[5]/div[1]/input");
    driver.findElement(by).click();

    // 20. Is 'Ödeme linki gönderilmiştir' visible?
    by = By.xpath("//p[. = 'Ödeme linki gönderilmiştir']");
    driver.findElement(by).isDisplayed();

    // 21. Output searched string in specific mail
    readSpecificsMail = ImapMailOperations.readSpecificsMail("netahsilatotomasyon@yandex.com","cishmttwpcryalzi","37653746593765","C:\\TestProject\\mail\\C2020.html");
    readSpecificsMail = (ImapMailOperations.ReadSpecificsMail)((ReportingDriver)driver).addons().execute(readSpecificsMail);

    // 22. Navigate to 'file:///C:/TestProject/mail/C2020.html'
    driver.navigate().to("file:///C:/TestProject/mail/C2020.html");

    // 23. Click 'dogrulamaLinki'
    by = By.xpath("//a");
    driver.findElement(by).click();

    // 24. Run Method Parametrik Ödeme (Sadece Kart Bilgisi)
    methodparametrikdemesadecekartbilgisi = new MethodParametrikdemesadeceKartBilgisi();
    methodparametrikdemesadecekartbilgisi.kartNo = "4506347027911094";
    methodparametrikdemesadecekartbilgisi.CVV = "000";
    methodparametrikdemesadecekartbilgisi.ayYil = "12/30";
    methodparametrikdemesadecekartbilgisi.execute(driver);

    // 25. Click '//td[contains(@class,'commRate-120')]//div[text()='5 x 13,31 ₺']'
    by = By.xpath("//td[contains(@class,'commRate-120')]//div[text()='5 x 13,31 ₺']");
    driver.findElement(by).click();

    // 26. Click 'odemeTalimatiOlustur'
    by = By.cssSelector("input[value='ÖDEME TALİMATI OLUŞTUR']");
    driver.findElement(by).click();

    // 27. Click 'btnOdemeyiTamamla'
    by = By.cssSelector("#btnNormalPayment");
    driver.findElement(by).click();

    // 28. Is 'İşlemden Vazgeçildi.Hata ID : ' visible?
    /* Step is disabled
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//p[contains(text(), 'İşlemden Vazgeçildi.Hata ID :')]");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    */
    // 29. Is 'Hata ID :' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//p[contains(text(), 'Hata ID :')]");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 30. Run Method Parametrik Ödeme (Sadece Kart Bilgisi)
    methodparametrikdemesadecekartbilgisi = new MethodParametrikdemesadeceKartBilgisi();
    methodparametrikdemesadecekartbilgisi.kartNo = "4506347027911094";
    methodparametrikdemesadecekartbilgisi.CVV = "000";
    methodparametrikdemesadecekartbilgisi.ayYil = "12/30";
    methodparametrikdemesadecekartbilgisi.execute(driver);

    // 31. Click '//td[contains(@class,'commRate-120')]//div[text()='5 x 13,31 ₺']'
    by = By.xpath("//td[contains(@class,'commRate-120')]//div[text()='5 x 13,31 ₺']");
    driver.findElement(by).click();

    // 32. Click 'odemeTalimatiOlustur'
    by = By.cssSelector("input[value='ÖDEME TALİMATI OLUŞTUR']");
    driver.findElement(by).click();

    // 33. Click 'btnOdemeyiTamamla'
    by = By.cssSelector("#btnNormalPayment");
    driver.findElement(by).click();

    // 34. Is 'İşlemden Vazgeçildi.Hata ID : ' visible?
    /* Step is disabled
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//p[contains(text(), 'İşlemden Vazgeçildi.Hata ID :')]");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    */
    // 35. Is 'Hata ID :' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//p[contains(text(), 'Hata ID :')]");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 36. Run Method Parametrik Ödeme (Sadece Kart Bilgisi)
    methodparametrikdemesadecekartbilgisi = new MethodParametrikdemesadeceKartBilgisi();
    methodparametrikdemesadecekartbilgisi.kartNo = "4506347027911094";
    methodparametrikdemesadecekartbilgisi.CVV = "000";
    methodparametrikdemesadecekartbilgisi.ayYil = "12/30";
    methodparametrikdemesadecekartbilgisi.execute(driver);

    // 37. Click '//td[contains(@class,'commRate-120')]//div[text()='5 x 13,31 ₺']'
    by = By.xpath("//td[contains(@class,'commRate-120')]//div[text()='5 x 13,31 ₺']");
    driver.findElement(by).click();

    // 38. Click 'odemeTalimatiOlustur'
    by = By.cssSelector("input[value='ÖDEME TALİMATI OLUŞTUR']");
    driver.findElement(by).click();

    // 39. Click 'btnOdemeyiTamamla'
    by = By.cssSelector("#btnNormalPayment");
    driver.findElement(by).click();

    // 40. Is 'İşlemden Vazgeçildi.Hata ID : ' visible?
    /* Step is disabled
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//p[contains(text(), 'İşlemden Vazgeçildi.Hata ID :')]");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    */
    // 41. Is 'Hata ID :' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//p[contains(text(), 'Hata ID :')]");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 42. Run Method Parametrik Ödeme (Sadece Kart Bilgisi)
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    methodparametrikdemesadecekartbilgisi = new MethodParametrikdemesadeceKartBilgisi();
    methodparametrikdemesadecekartbilgisi.kartNo = "4506347027911094";
    methodparametrikdemesadecekartbilgisi.CVV = "000";
    methodparametrikdemesadecekartbilgisi.ayYil = "12/30";
    methodparametrikdemesadecekartbilgisi.execute(driver);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 43. Click '//td[contains(@class,'commRate-120')]//div[text()='5 x 13,31 ₺']'
    by = By.xpath("//td[contains(@class,'commRate-120')]//div[text()='5 x 13,31 ₺']");
    driver.findElement(by).click();

    // 44. Click 'odemeTalimatiOlustur'
    by = By.cssSelector("input[value='ÖDEME TALİMATI OLUŞTUR']");
    driver.findElement(by).click();

    // 45. Click 'btnOdemeyiTamamla'
    by = By.cssSelector("#btnNormalPayment");
    driver.findElement(by).click();

    // 46. Does 'Bu email order daha önce ödenmiş veya...' contain 'Bu email order daha önce ödenmiş veya maksimum hata sayısına ulaşmış olabilir. Lütfen firmanızla iletişime geçiniz.'?
    by = By.xpath("//li[. = 'Bu email order daha önce ödenmiş veya maksimum hata sayısına ulaşmış olabilir. Lütfen firmanızla iletişime geçiniz.']");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Bu email order daha önce ödenmiş veya maksimum hata sayısına ulaşmış olabilir. Lütfen firmanızla iletişime geçiniz."));

    // 47. Navigate to 'file:///C:/TestProject/mail/C2020.html'
    driver.navigate().to("file:///C:/TestProject/mail/C2020.html");

    // 48. Click 'dogrulamaLinki'
    by = By.xpath("//a");
    driver.findElement(by).click();

    // 49. Get text from 'Hata: Bu mail order ödemesi 3 kez baş...'
    by = By.cssSelector("body > div:nth-child(5) > div > div > div > div");
    string = driver.findElement(by).getAttribute("value");
    Assertions.assertTrue(string.contains("3 kez başarısız"));

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon.netahsilatdemo.com/Account/LogOff","otomasyon@test.com","Kd!cyhz4","","",""));
  }
}
