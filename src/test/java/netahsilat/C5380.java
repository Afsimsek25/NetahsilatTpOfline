package netahsilat;

import io.testproject.addon.NetahsilatUtills;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C5380 Müşteri - Çoklu Seçim Müşteri Temsilcisi Atama (Yeni Kayıt ve Kayıt Düzenleme (NTH-14633)
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Mon Oct 10 08:34:22 GMT 2022.
 */
@DisplayName("C5380 Müşteri - Çoklu Seçim Müşteri Temsilcisi Atama (Yeni Kayıt ve Kayıt Düzenleme (NTH-14633)")
public class C5380 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C5380 Müşteri - Çoklu Seçim Müşteri Temsilcisi Atama (Yeni Kayıt ve Kayıt Düzenleme (NTH-14633)")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String password, String currentDateTime) throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogin methodlogin;
    NetahsilatUtills.ClickUntilGone clickUntilGone;

    // 1. Navigate to '{{Otomasyon2Logout}}'
    //    Navigates the specified URL (Auto-generated)
    driver.navigate().to(ProjectParameters.Otomasyon2Logout);

    // 2. Run Method Login
    //    admin@netahsilat.com
    methodlogin = new MethodLogin();
    methodlogin.ApplicationURL = ApplicationURL;
    methodlogin.userName = "admin@netahsilat.com";
    methodlogin.password = ProjectParameters.genelPassword;
    methodlogin.execute(driver);
    currentDateTime = String.valueOf(methodlogin.currentDateTime);

    // 3. Move mouse to 'moveUstUyeYonetimi'
    by = By.cssSelector("li[class='content']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 4. Click 'ustUyeYonetimi'
    by = By.xpath("//li[4]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();

    // 5. Click 'ÜYE YÖNETİMİ'
    by = By.xpath("//li[1]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();

    // 6. Click 'ddYeniUyeTanimi'
    by = By.xpath("//div[2]/div/img");
    driver.findElement(by).click();

    // 7. Click 'Yeni Müşteri Tanımla'
    by = By.xpath("//a[. = 'Yeni Müşteri Tanımla']");
    driver.findElement(by).click();

    // 8. Type 'coklumusteritemsilcisi@c5380.musteri' in 'Entity.VendorUser.Email'
    by = By.cssSelector("#Entity_VendorUser_Email");
    driver.findElement(by).sendKeys("coklumusteritemsilcisi@c5380.musteri");

    // 9. Type 'C5380' in 'Entity.VendorUser.Name'
    by = By.cssSelector("#Entity_VendorUser_Name");
    driver.findElement(by).sendKeys("C5380");

    // 10. Type 'Oğuzhan' in 'Entity.VendorUser.Surname'
    by = By.cssSelector("#Entity_VendorUser_Surname");
    driver.findElement(by).sendKeys("Oğuzhan");

    // 11. Type '11111111110' in 'Entity.Tckn'
    by = By.cssSelector("#Entity_Tckn");
    driver.findElement(by).sendKeys("11111111110");

    // 12. Type '{{password}}' in 'Entity.EditPassword'
    by = By.cssSelector("#Entity_EditPassword");
    driver.findElement(by).sendKeys(password);

    // 13. Type '{{password}}' in 'Entity.EditRePassword'
    by = By.cssSelector("#Entity_EditRePassword");
    driver.findElement(by).sendKeys(password);

    // 14. Type 'Site Admin Otomasyon2' in 's2id_autogen2'
    by = By.cssSelector("#s2id_autogen2");
    driver.findElement(by).sendKeys("Site Admin Otomasyon2");

    // 15. Click 'Site Admin Otomasyon2'
    by = By.xpath("//span[. = 'Site Admin Otomasyon2']");
    driver.findElement(by).click();

    // 16. Type 'Site Müşteri Temsilcisi 1' in 's2id_autogen2'
    by = By.cssSelector("#s2id_autogen2");
    driver.findElement(by).sendKeys("Site Müşteri Temsilcisi 1");

    // 17. Click 'Site Müşteri Temsilcisi 1'
    by = By.xpath("//li[1]//span[. = 'Site Müşteri Temsilcisi 1']");
    driver.findElement(by).click();

    // 18. Run Click Until Gone
    clickUntilGone = NetahsilatUtills.clickUntilGone(30);
    by = By.xpath("//button[. = 'Tamam']");
    clickUntilGone = (NetahsilatUtills.ClickUntilGone)((ReportingDriver)driver).addons().execute(clickUntilGone, by, -1);

    // 19. Type 'coklumusteritemsilcisi@c5380.musteri' in 'SearchVendorName'
    by = By.cssSelector("#SearchVendorName");
    driver.findElement(by).sendKeys("coklumusteritemsilcisi@c5380.musteri");

    // 20. Click 'resultListelenen'
    by = By.xpath("//body/ul//div");
    driver.findElement(by).click();

    // 21. Click 'Listele'
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();

    // 22. Is 'Listele' is clickable?
    by = By.xpath("//button[. = 'Listele']");
    (new WebDriverWait(driver, 15)).until(ExpectedConditions.elementToBeClickable(by));

    // 23. Click 'edit.png'
    by = By.xpath("//img[contains(@src,'edit.png')]");
    driver.findElement(by).click();

    // 24. Is 'Site Admin Otomasyon21' visible?
    by = By.xpath("//div[. = 'Site Admin Otomasyon2']");
    driver.findElement(by).isDisplayed();

    // 25. Is 'Site Müşteri Temsilcisi 1 TextboxArea' visible?
    by = By.xpath("//ul[@class='select2-choices']/.//li//div[text()='Site Müşteri Temsilcisi 1']");
    driver.findElement(by).isDisplayed();

    // 26. Click 'Site Admin Otomasyon2 closeBtn'
    by = By.xpath("//div[text()='Site Admin Otomasyon2']/..//a[@class='select2-search-choice-close']");
    driver.findElement(by).click();

    // 27. Click 'Site Müşteri Temsilcisi 1 close btn'
    by = By.xpath("//div[text()='Site Müşteri Temsilcisi 1']//../a[@class='select2-search-choice-close']");
    driver.findElement(by).click();

    // 28. Type 'admin@netahsilat.com' in 'Müşteri Temsilcisi Textbox'
    by = By.xpath("//div[@id='tab-userinfo']//input[contains(@class, 'select2-input')]");
    driver.findElement(by).sendKeys("admin@netahsilat.com");

    // 29. Click 'admin@netahsilat.com'
    by = By.xpath("//span[. = 'admin@netahsilat.com']");
    driver.findElement(by).click();

    // 30. Type 'otomasyon test' in 'Müşteri Temsilcisi Textbox'
    by = By.xpath("//div[@id='tab-userinfo']//input[contains(@class, 'select2-input')]");
    driver.findElement(by).sendKeys("otomasyon test");

    // 31. Click 'otomasyon test'
    by = By.xpath("//div/span[. = 'otomasyon test']");
    driver.findElement(by).click();

    // 32. Run Click Until Gone
    clickUntilGone = NetahsilatUtills.clickUntilGone(30);
    by = By.xpath("//button[. = 'Tamam']");
    clickUntilGone = (NetahsilatUtills.ClickUntilGone)((ReportingDriver)driver).addons().execute(clickUntilGone, by, -1);

    // 33. Click 'edit.png'
    by = By.xpath("//img[contains(@src,'edit.png')]");
    driver.findElement(by).click();

    // 34. Is 'admin@netahsilat.com test' visible?
    by = By.xpath("//div[. = 'admin@netahsilat.com test']");
    driver.findElement(by).isDisplayed();

    // 35. Is 'otomasyon test1' visible?
    by = By.xpath("//li/div[. = 'otomasyon test']");
    driver.findElement(by).isDisplayed();

    // 36. Run Click Until Gone
    clickUntilGone = NetahsilatUtills.clickUntilGone(30);
    by = By.xpath("//button[. = 'Vazgeç']");
    clickUntilGone = (NetahsilatUtills.ClickUntilGone)((ReportingDriver)driver).addons().execute(clickUntilGone, by, -1);

    // 37. Click 'Oguzhan dal Trash.png'
    by = By.xpath("//td[contains(text(),'C5380 Oğuzhan')]//..//img[contains(@src,'trash.png')]");
    driver.findElement(by).click();

    // 38. Run Click Until Gone
    clickUntilGone = NetahsilatUtills.clickUntilGone(30);
    by = By.xpath("//button[. = 'Tamam']");
    clickUntilGone = (NetahsilatUtills.ClickUntilGone)((ReportingDriver)driver).addons().execute(clickUntilGone, by, -1);

    // 39. Is 'Gösterilecek Kayıt Bulunamadı.2' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//div[3]/div/table//td[. = 'Gösterilecek Kayıt Bulunamadı.']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon2.netahsilatdemo.com/","Kd!cyhz4",""));
  }
}
