package netahsilat;

import io.testproject.addon.GridOperations;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C402 Kullanıcı İle Listeleme
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Thu Sep 29 08:57:23 GMT 2022.
 */
@DisplayName("C402 Kullanıcı İle Listeleme")
public class C402 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C402 Kullanıcı İle Listeleme")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String userName, String password) throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogoutlogin methodlogoutlogin;
    GridOperations.GetData getData;

    // 1. Run Method Logout&Login
    GeneratedUtils.sleep(500);
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = ProjectParameters.genelEmail;
    methodlogoutlogin.password = ProjectParameters.genelPassword;
    methodlogoutlogin.execute(driver);

    // 2. Move mouse to 'moveToIslemler'
    by = By.cssSelector("li[class='tag']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 3. Click 'İŞLEMLER'
    by = By.cssSelector("a[title='İŞLEMLER']");
    driver.findElement(by).click();

    // 4. Click 'İŞLEM LİSTESİ'
    by = By.cssSelector("a[title='İŞLEM LİSTESİ']");
    driver.findElement(by).click();

    // 5. Is 'btnListele' is clickable?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//button[. = 'Listele']");
    (new WebDriverWait(driver, 35)).until(ExpectedConditions.elementToBeClickable(by));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 6. Type 'C402 User Surname' in 'AppUserName'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.cssSelector("#AppUserName");
    driver.findElement(by).sendKeys("C402 User Surname");
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 7. Is 'C402 User Name C402 User Surname' visible?
    by = By.xpath("//div[contains(text(),'C402 User Name C402 User Surname,')]");
    driver.findElement(by).isDisplayed();

    // 8. Is 'TestAd SoyadTest,76012133968' visible?
    /* Step is disabled
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("/html/body/ul[4]/li/div");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    */
    // 9. Clear 'AppUserName' contents
    by = By.cssSelector("#AppUserName");
    driver.findElement(by).clear();

    // 10. Type 'C402 User Name' in 'AppUserName'
    by = By.cssSelector("#AppUserName");
    driver.findElement(by).sendKeys("C402 User Name");

    // 11. Is 'C402 User Name C402 User Surname' visible?
    by = By.xpath("//div[contains(text(),'C402 User Name C402 User Surname,')]");
    driver.findElement(by).isDisplayed();

    // 12. Clear 'AppUserName' contents
    by = By.cssSelector("#AppUserName");
    driver.findElement(by).clear();

    // 13. Type 'bireyselanabayi@otomasyon.com' in 'AppUserName'
    by = By.cssSelector("#AppUserName");
    driver.findElement(by).sendKeys("bireyselanabayi@otomasyon.com");

    // 14. Is 'Bireysel Ana Bayi,76162482466' visible?
    by = By.xpath("//div[contains(text(),',76162482466')]");
    driver.findElement(by).isDisplayed();

    // 15. Clear 'AppUserName' contents
    by = By.cssSelector("#AppUserName");
    driver.findElement(by).clear();

    // 16. Type '76162482466' in 'AppUserName'
    by = By.cssSelector("#AppUserName");
    driver.findElement(by).sendKeys("76162482466");

    // 17. Is 'resultListelenen' visible?
    by = By.xpath("//body/ul//div");
    driver.findElement(by).isDisplayed();

    // 18. Clear 'AppUserName' contents
    by = By.cssSelector("#AppUserName");
    driver.findElement(by).clear();

    // 19. Type 'BireyselAnaBayi' in 'AppUserName'
    by = By.cssSelector("#AppUserName");
    driver.findElement(by).sendKeys("BireyselAnaBayi");

    // 20. Is 'Bireysel Ana Bayi,76162482466' visible?
    by = By.xpath("//div[contains(text(),',76162482466')]");
    driver.findElement(by).isDisplayed();


    Thread.sleep(3000);
    // 21. Click 'resultListelenen'
    by = By.xpath("//body/ul//div");
    driver.findElement(by).click();


    // 22. Clear 'StartDate' contents
    by = By.cssSelector("#StartDate");
    driver.findElement(by).clear();

    // 23. Click 'btnListele'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 24. Is 'btnListele' is clickable?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//button[. = 'Listele']");
    (new WebDriverWait(driver, 35)).until(ExpectedConditions.elementToBeClickable(by));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 25. Run Get Data From Grid
    //    Üye ErpKodu - Bayi123 - Equals
    getData = GridOperations.getData("Üye ErpKodu","BireyselAnaBayiErp","Equals");
    getData = (GridOperations.GetData)((ReportingDriver)driver).addons().execute(getData);

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon.netahsilatdemo.com/Account/LogOff","",""));
  }
}
