package netahsilat;

import io.testproject.addon.ImapMailOperations;
import io.testproject.addon.TypeTextSlowly;
import io.testproject.addon.WebExtensions;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C3241 MS - Kart kayıt linki üzerinden başarılı kayıt sonrası aynı link ile tekrar kayıt yapılmaması
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Fri Oct 07 11:31:46 GMT 2022.
 */
@DisplayName("C3241 MS - Kart kayıt linki üzerinden başarılı kayıt sonrası aynı link ile tekrar kayıt yapılmaması")
public class C3241 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C3241 MS - Kart kayıt linki üzerinden başarılı kayıt sonrası aynı link ile tekrar kayıt yapılmaması")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL) throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogin methodlogin;
    ImapMailOperations.ReadSpecificsMail readSpecificsMail;
    TypeTextSlowly.TypeTextWithControlledSpeed typeTextWithControlledSpeed;
    ImapMailOperations.DeleteAllMails deleteAllMails;
    WebExtensions.SetElementAttribute setElementAttribute;

    // 1. Navigate to '{{Otomasyon3Logout}}'
    driver.navigate().to(ProjectParameters.Otomasyon3Logout);

    // 2. Run Method Login
    methodlogin = new MethodLogin();
    methodlogin.ApplicationURL = ApplicationURL;
    methodlogin.userName = ProjectParameters.genelEmail;
    methodlogin.password = ProjectParameters.genelPassword;
    methodlogin.execute(driver);

    // 3. Delete All Mails
    deleteAllMails = ImapMailOperations.deleteAllMails(ProjectParameters.odemeEmail,ProjectParameters.yandexMailKey);
    deleteAllMails = (ImapMailOperations.DeleteAllMails)((ReportingDriver)driver).addons().execute(deleteAllMails);

    // 4. Move mouse to 'moveUstUyeYonetimi'
    by = By.cssSelector("li[class='content']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 5. Click 'ustUyeYonetimi'
    by = By.xpath("//li[4]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();

    // 6. Click 'altUyeYonetimi'
    by = By.xpath("//li[1]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();

    // 7. Type 'netahsilatotomasyon@yandex.com' in 'SearchVendorName'
    by = By.cssSelector("#SearchVendorName");
    driver.findElement(by).sendKeys("netahsilatotomasyon@yandex.com");

    // 8. Click 'resultListelenen'
    by = By.xpath("//body/ul//div");
    driver.findElement(by).click();

    // 9. Click 'Listele'
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();

    // 10. Is 'Listele' is clickable?
    by = By.xpath("//button[. = 'Listele']");
    (new WebDriverWait(driver, 15)).until(ExpectedConditions.elementToBeClickable(by));

    // 11. Set 'target' attribute value on 'Ödeme'
    setElementAttribute = WebExtensions.setElementAttribute("target","_top");
    by = By.xpath("//a[. = 'Ödeme']");
    setElementAttribute = (WebExtensions.SetElementAttribute)((ReportingDriver)driver).addons().execute(setElementAttribute, by, -1);

    // 12. Click 'Ödeme'
    by = By.xpath("//a[. = 'Ödeme']");
    driver.findElement(by).click();

    // 13. Click 'KART KAYDI LİNKİ'
    by = By.xpath("//a[. = 'KART KAYDI LİNKİ']");
    driver.findElement(by).click();

    // 14. Click 'EmailTypeSelection'
    by = By.cssSelector("#EmailTypeSelection");
    driver.findElement(by).click();

    // 15. Does 'netahsilatotomasyon@yandex.com' contain 'netahsilatotomasyon@yandex.com'?
    by = By.xpath("//td[. = 'netahsilatotomasyon@yandex.com']");
    Assertions.assertTrue(driver.findElement(by).getText().contains("netahsilatotomasyon@yandex.com"));

    // 16. Click 'Gönder'
    by = By.xpath("//button[. = 'Gönder']");
    driver.findElement(by).click();

    // 17. Is 'İşleminiz başarıyla tamamlanmıştır' present?
    by = By.xpath("//h4[. = 'İşleminiz başarıyla tamamlanmıştır']");
    driver.findElement(by);

    // 18. Is 'Ana Sayfa' present?
    by = By.xpath("//a[. = 'Ana Sayfa']");
    driver.findElement(by);

    // 19. Is 'Kart Kayıt Linki Gönder' present?
    by = By.xpath("//a[. = 'Kart Kayıt Linki Gönder ']");
    driver.findElement(by);

    // 20. Output searched string in specific mail
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    readSpecificsMail = ImapMailOperations.readSpecificsMail(ProjectParameters.odemeEmail,ProjectParameters.yandexMailKey,"E-Çözüm","C:/TestProject/mail/C3241.html");
    readSpecificsMail = (ImapMailOperations.ReadSpecificsMail)((ReportingDriver)driver).addons().execute(readSpecificsMail);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 21. Navigate to 'file:///C:/TestProject/mail/C3241.html'
    driver.navigate().to("file:///C:/TestProject/mail/C3241.html");

    // 22. Click 'Kart Kaydı yapmak için lütfen tıklayı...'
    by = By.xpath("//a[. = 'Kart Kaydı yapmak için lütfen tıklayınız...']");
    driver.findElement(by).click();

    // 23. Type 'KartLinkSave' in 'Kart üzerindeki isim'
    by = By.cssSelector("#CardHolderName");
    driver.findElement(by).sendKeys("KartLinkSave");

    // 24. Type '11111111110' in 'TKC'
    by = By.cssSelector("#TKC");
    driver.findElement(by).sendKeys("11111111110");

    // 25. Type '5555555555' in 'Phone'
    by = By.cssSelector("#Phone");
    driver.findElement(by).sendKeys("5555555555");

    // 26. Type 'kartlink@test.com' in 'Email'
    by = By.cssSelector("#Email");
    driver.findElement(by).sendKeys("kartlink@test.com");

    // 27. Type text to a TextField element with a delay between characters.
    typeTextWithControlledSpeed = TypeTextSlowly.typeTextWithControlledSpeed("5571135571135575",0);
    by = By.cssSelector("#CardNumber");
    typeTextWithControlledSpeed = (TypeTextSlowly.TypeTextWithControlledSpeed)((ReportingDriver)driver).addons().execute(typeTextWithControlledSpeed, by, -1);

    // 28. Click 'Kart No'
    by = By.cssSelector("#CardNumber");
    driver.findElement(by).click();

    // 29. Type text to a TextField element with a delay between characters.
    typeTextWithControlledSpeed = TypeTextSlowly.typeTextWithControlledSpeed("12/24",0);
    by = By.cssSelector("#CardExpireDate");
    typeTextWithControlledSpeed = (TypeTextSlowly.TypeTextWithControlledSpeed)((ReportingDriver)driver).addons().execute(typeTextWithControlledSpeed, by, -1);

    // 30. Type '111' in 'CVV'
    by = By.cssSelector("#CardCvv");
    driver.findElement(by).sendKeys("111");

    // 31. Click 'Kaydet'
    by = By.xpath("//button[. = 'Kaydet']");
    driver.findElement(by).click();

    // 32. Is '55************75 Sil Btn' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("(//td[contains(text(),'55************75')])/..//a[text()='Sil']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 33. Navigate to '{{Otomasyon3Logout}}'
    driver.navigate().to(ProjectParameters.Otomasyon3Logout);

    // 34. Output searched string in specific mail
    /* Step is disabled
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    readSpecificsMail = ImapMailOperations.readSpecificsMail(ProjectParameters.odemeEmail,ProjectParameters.yandexMailKey,"E-Çözüm","C:/TestProject/mail/C3241.html");
    readSpecificsMail = (ImapMailOperations.ReadSpecificsMail)((ReportingDriver)driver).addons().execute(readSpecificsMail);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    */
    // 35. Navigate to 'file:///C:/TestProject/mail/C3241.html'
    driver.navigate().to("file:///C:/TestProject/mail/C3241.html");

    // 36. Click 'Kart Kaydı yapmak için lütfen tıklayı...'
    by = By.xpath("//a[. = 'Kart Kaydı yapmak için lütfen tıklayınız...']");
    driver.findElement(by).click();

    // 37. Does 'Hata: Bu link artık kullanılamaz. Lüt...' contain 'Hata: Bu link artık kullanılamaz. Lütfen tekrar link oluşturunuz.'?
    by = By.xpath("//p[. = '\n                            Hata:  Bu link artık kullanılamaz. Lütfen tekrar link oluşturunuz. \n                        ']");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Hata: Bu link artık kullanılamaz. Lütfen tekrar link oluşturunuz."));

    // 38. Navigate to '{{Otomasyon3Logout}}'
    driver.navigate().to(ProjectParameters.Otomasyon3Logout);

    // 39. Run Method Login
    methodlogin = new MethodLogin();
    methodlogin.ApplicationURL = ApplicationURL;
    methodlogin.userName = ProjectParameters.odemeEmail;
    methodlogin.password = ProjectParameters.genelPassword;
    methodlogin.execute(driver);

    // 40. Click 'KREDI KARTLARIM'
    by = By.xpath("//a[. = 'Kredi kartlarım']");
    driver.findElement(by).click();

    // 41. Click 'Güncelle'
    by = By.xpath("//*[starts-with(text(),'Ka*')]/..//following::td//a[text()='Güncelle']");
    driver.findElement(by).click();

    // 42. Does 'TKC' contain '[NONE]'?
    by = By.cssSelector("#TKC");
    Assertions.assertTrue(driver.findElement(by).getText().contains(""));

    // 43. Does 'Phone' contain '[NONE]'?
    by = By.cssSelector("#Phone");
    Assertions.assertTrue(driver.findElement(by).getText().contains(""));

    // 44. Does 'Email' contain '[NONE]'?
    by = By.cssSelector("#Email");
    Assertions.assertTrue(driver.findElement(by).getText().contains(""));

    // 45. Click 'KREDI KARTLARIM'
    by = By.xpath("//a[. = 'Kredi kartlarım']");
    driver.findElement(by).click();

    // 46. Click '55************75 Sil Btn'
    by = By.xpath("(//td[contains(text(),'55************75')])/..//a[text()='Sil']");
    driver.findElement(by).click();

    // 47. Click 'Sil1'
    by = By.xpath("//button[. = 'Sil']");
    driver.findElement(by).click();

    // 48. Is '55************75 Sil Btn' not present or invisible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("(//td[contains(text(),'55************75')])/..//a[text()='Sil']");
    (new WebDriverWait(driver, 2)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon3.netahsilatdemo.com/"));
  }
}
