package netahsilat;

import io.testproject.addon.*;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C2069 KrediKartSahibi dinamik alanının eklenmesi (Ödeme Linki ile) (NTH-15607)
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Fri Oct 07 11:17:53 GMT 2022.
 */
@DisplayName("C2069 KrediKartSahibi dinamik alanının eklenmesi (Ödeme Linki ile) (NTH-15607)")
public class C2069 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C2069 KrediKartSahibi dinamik alanının eklenmesi (Ödeme Linki ile) (NTH-15607)")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String userName, String password, String kartNo, String CVV,
      String ayYil, String odemeLinki, String randomNumber) throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogoutlogin methodlogoutlogin;
    WebExtensions.SelectOptionbyVisibleText selectOptionbyVisibleText;
    ImapMailOperations.ReadSpecificsMail readSpecificsMail;
    CombinedActions.ClearAndTypeAction clearAndTypeAction;
    RandomDataGenerator.GenerateNumberInRange generateNumberInRange;
    ImapMailOperations.DeleteAllMails deleteAllMails;
    WebExtensions.GetElementAttribute getElementAttribute;
    VisibleElementsOperations.ClickIfVisibleWeb clickIfVisibleWeb;

    // 1. Run Method Logout&Login
    GeneratedUtils.sleep(500);
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = ProjectParameters.genelEmail;
    methodlogoutlogin.password = ProjectParameters.genelPassword;
    methodlogoutlogin.execute(driver);

    // 2. Delete All Mails
    deleteAllMails = ImapMailOperations.deleteAllMails(ProjectParameters.odemeEmail,ProjectParameters.yandexMailKey);
    deleteAllMails = (ImapMailOperations.DeleteAllMails)((ReportingDriver)driver).addons().execute(deleteAllMails);

    // 3. Move mouse to 'moveUstUyeYonetimi'
    by = By.cssSelector("li[class='content']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 4. Click 'moveUstUyeYonetimi'
    by = By.cssSelector("li[class='content']");
    driver.findElement(by).click();

    // 5. Click 'dropDownOdemeLinki'
    by = By.xpath("//li[2]/div/img");
    driver.findElement(by).click();

    // 6. Click 'ÜYELİKSİZ ÖDEME LİNKİ'
    by = By.xpath("//a[. = 'ÜYELİKSİZ ÖDEME LİNKİ']");
    driver.findElement(by).click();

    // 7. Run Clear 'PureAmount' contents and type '100'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("100");
    by = By.cssSelector("#PureAmount");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 8. Generate random number between '10000000' and '99999999'
    generateNumberInRange = RandomDataGenerator.generateNumberInRange("10000000","99999999");
    generateNumberInRange = (RandomDataGenerator.GenerateNumberInRange)((ReportingDriver)driver).addons().execute(generateNumberInRange);
    randomNumber = String.valueOf(generateNumberInRange.result);

    // 9. Type '{{randomNumber}}' in 'CustomerId'
    by = By.cssSelector("#CustomerId");
    driver.findElement(by).sendKeys(randomNumber);

    // 10. Click 'Devam et Button'
    by = By.cssSelector("input[value='Devam Et']");
    driver.findElement(by).click();

    // 11. Type 'User' in 'inputUnvan'
    by = By.cssSelector("#DisplayName");
    driver.findElement(by).sendKeys("User");

    // 12. Type 'Özge Güngördü ' in 'FullName'
    by = By.cssSelector("#FullName");
    driver.findElement(by).sendKeys("Özge Güngördü ");

    // 13. Type '{{odemeEmail}}' in 'EMail'
    by = By.cssSelector("#EMail");
    driver.findElement(by).sendKeys(ProjectParameters.odemeEmail);

    // 14. Click 'Ekle'
    by = By.xpath("//button[. = 'Ekle']");
    driver.findElement(by).click();

    // 15. Is 'TABLE' visible?
    by = By.xpath("//div[3]/table");
    driver.findElement(by).isDisplayed();

    // 16. Click 'UnvanEkle2'
    by = By.xpath("//td[1]/input[@type=\"checkbox\"][@data-bind=\"visible: $$root.emailOrSmsCheckEnable(), checked: isChecked\"]");
    driver.findElement(by).click();

    // 17. Type '{{randomNumber}}' in 'Description'
    by = By.cssSelector("#Description");
    driver.findElement(by).sendKeys(randomNumber);

    // 18. Click 'emailGonder'
    by = By.xpath("//div[5]/div[1]/input");
    driver.findElement(by).click();

    // 19. Output searched string in specific mail
    readSpecificsMail = ImapMailOperations.readSpecificsMail(ProjectParameters.odemeEmail,ProjectParameters.yandexMailKey,randomNumber,"C:\\TestProject\\mail\\C2069.html");
    readSpecificsMail = (ImapMailOperations.ReadSpecificsMail)((ReportingDriver)driver).addons().execute(readSpecificsMail);

    // 20. Navigate to 'file:///C:/TestProject/mail/C2069.html'
    driver.navigate().to("file:///C:/TestProject/mail/C2069.html");

    // 21. Get 'href' attribute value on 'dogrulamaLinki'
    getElementAttribute = WebExtensions.getElementAttribute("href");
    by = By.xpath("//a");
    getElementAttribute = (WebExtensions.GetElementAttribute)((ReportingDriver)driver).addons().execute(getElementAttribute, by, -1);
    odemeLinki = String.valueOf(getElementAttribute.attributeValue);

    // 22. Navigate to '{{odemeLinki}}'
    driver.navigate().to(odemeLinki);

    // 23. Does 'User2' contain 'User'?
    by = By.cssSelector("#MAILSMSORDERUNVAN");
    Assertions.assertTrue(driver.findElement(by).getText().contains("User"));

    // 24. Does 'MAILSMSORDERMUSTERINO' contain '{{randomNumber}}'?
    by = By.cssSelector("#MAILSMSORDERMUSTERINO");
    Assertions.assertTrue(driver.findElement(by).getText().contains(randomNumber));

    // 25. Run Method Logout&Login
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = userName;
    methodlogoutlogin.password = password;
    methodlogoutlogin.execute(driver);

    // 26. Move mouse to 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 27. Click 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    driver.findElement(by).click();

    // 28. Click 'btnParametreler'
    by = By.cssSelector("a[title='PARAMETRELER']");
    driver.findElement(by).click();

    // 29. Click 'li > a > Ön Bilgilendirme Formu'
    by = By.xpath("//a[normalize-space()='Ön Bilgilendirme Formu']");
    driver.findElement(by).click();

    // 30. Select options in 'PreInformationForm.Visible' with text '[NONE]'
    selectOptionbyVisibleText = WebExtensions.selectOptionbyVisibleText("Pasif");
    by = By.cssSelector("[name='PreInformationForm.Visible']");
    selectOptionbyVisibleText = (WebExtensions.SelectOptionbyVisibleText)((ReportingDriver)driver).addons().execute(selectOptionbyVisibleText, by, -1);

    // 31. Click 'Müşteri (Unchecked)' if it's visible
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("0");
    by = By.xpath("//label[normalize-space()='Müşteri']//..//following-sibling::div//span[@class='jquery-safari-checkbox jquery-safari-checkbox-checked']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 32. Click 'Üyeliksiz (Unchecked)' if it's visible
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("0");
    by = By.xpath("//label[normalize-space()='Üyeliksiz']//..//following-sibling::div//span[@class='jquery-safari-checkbox jquery-safari-checkbox-checked']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 33. Is 'editorToolbar' visible?
    by = By.xpath("//div[contains(@id,'toolbargroup') and not(contains(@id,'Locales' ))]");
    driver.findElement(by).isDisplayed();

    // 34. Click 'Kaydet1'
    by = By.cssSelector("#kaydet");
    driver.findElement(by).click();

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon.netahsilatdemo.com/Account/LogOff","otomasyon@test.com","Kd!cyhz4","","","","",""));
  }
}
