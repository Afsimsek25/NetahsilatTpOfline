package netahsilat;

import io.testproject.addon.NetahsilatUtills;
import io.testproject.addon.VisibleElementsOperations;
import io.testproject.addon.WebExtensions;
import io.testproject.addon.WebListOperations;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C754 Kullanıcı sözleşmesi , ilk girişte Şifre /yeni kayıt olarak da denenmeli /sözleşmenin dile göre gelmesi önemli
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Fri Oct 07 11:09:01 GMT 2022.
 */
@DisplayName("C754 Kullanıcı sözleşmesi , ilk girişte Şifre /yeni kayıt olarak da denenmeli /sözleşmenin dile göre gelmesi önemli")
public class C754 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C754 Kullanıcı sözleşmesi , ilk girişte Şifre /yeni kayıt olarak da denenmeli /sözleşmenin dile göre gelmesi önemli")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String userName, String password) throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodyeSilme methodyesilme;
    MethodBireyselMteriEkle methodbireyselmteriekle;
    MethodLogoutlogin methodlogoutlogin;
    WebExtensions.SelectOptionbyVisibleText selectOptionbyVisibleText;
    NetahsilatUtills.ClickUntilGone clickUntilGone;
    VisibleElementsOperations.ClickIfVisibleWeb clickIfVisibleWeb;
    WebListOperations.GetTextFromSelectedItem getTextFromSelectedItem;

    // 1. Run Method Logout&Login
    GeneratedUtils.sleep(500);
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = ProjectParameters.genelEmail;
    methodlogoutlogin.password = ProjectParameters.genelPassword;
    methodlogoutlogin.execute(driver);

    // 2. Move mouse to 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 3. Click 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    driver.findElement(by).click();

    // 4. Click 'btnParametreler'
    by = By.cssSelector("a[title='PARAMETRELER']");
    driver.findElement(by).click();

    // 5. Click 'Kullanıcı Sözleşmesi'
    by = By.xpath("//a[. = '\n            Kullanıcı Sözleşmesi\n        ']");
    driver.findElement(by).click();

    // 6. Is 'editorToolbar' visible?
    by = By.xpath("//div[contains(@id,'toolbargroup') and not(contains(@id,'Locales' ))]");
    driver.findElement(by).isDisplayed();

    // 7. Click 'Kullanıcılar ilk girişlerinde, şifrelerini değiştirsinler(Unchecked)' if it's visible
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("");
    by = By.xpath("//label[normalize-space()='Kullanıcılar ilk girişlerinde, şifrelerini değiştirsinler']/../..//..//span[@class='jquery-safari-checkbox']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);

    // 8. Click 'Kullanıcılar ilk girişlerinde, üyelik talimat formunu onaylasınlar (Unchecked)' if it's visible
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("");
    by = By.xpath("//label[normalize-space()='Kullanıcılar ilk girişlerinde, üyelik talimat formunu onaylasınlar']/../..//..//span[@class='jquery-safari-checkbox']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);

    // 9. Click 'Kaydet1'
    by = By.cssSelector("#kaydet");
    driver.findElement(by).click();

    // 10. Run Method Bireysel Müşteri Ekle
    methodbireyselmteriekle = new MethodBireyselMteriEkle();
    methodbireyselmteriekle.Adi = "C754";
    methodbireyselmteriekle.Soyadi = "Müşteri";
    methodbireyselmteriekle.Sifre = ProjectParameters.genelPassword;
    methodbireyselmteriekle.EPosta = "c754@musteri.com";
    methodbireyselmteriekle.Uyruk = "Turkey";
    methodbireyselmteriekle.Tckn = "72134750458";
    methodbireyselmteriekle.musteriTemsilcisi = "admin@netahsilat.com";
    methodbireyselmteriekle.uyeKodu = "C754BireyselMusteri";
    methodbireyselmteriekle.erpKodu = "C754BireyselMusteriERP";
    methodbireyselmteriekle.execute(driver);

    // 11. Run Method Logout&Login
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = "c754@musteri.com";
    methodlogoutlogin.password = ProjectParameters.genelPassword;
    methodlogoutlogin.execute(driver);

    // 12. Does 'Şifre Değişikliği' contain 'Şifre Değişikliği'?
    by = By.cssSelector("#headings");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Şifre Değişikliği"));

    // 13. Type '{{genelPassword}}123' in 'ŞifreAlanı'
    by = By.cssSelector("#Password");
    driver.findElement(by).sendKeys(String.format("%s123",ProjectParameters.genelPassword));

    // 14. Type '{{genelPassword}}123' in 'RepeatPassword'
    by = By.cssSelector("#RepeatPassword");
    driver.findElement(by).sendKeys(String.format("%s123",ProjectParameters.genelPassword));

    // 15. Click 'Kaydet'
    by = By.xpath("//button[. = 'Kaydet']");
    driver.findElement(by).click();

    // 16. Does 'Türkçe Kullanıcı Sözleşmesi' contain 'Türkçe Kullanıcı Sözleşmesi'?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(25000, TimeUnit.MILLISECONDS);
    by = By.xpath("//p[contains(text(),'Türkçe Kullanıcı Sözleşmesi')]");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Türkçe Kullanıcı Sözleşmesi"));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 17. Is 'Sözleşme Yazdır Butonu' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(100, TimeUnit.MILLISECONDS);
    by = By.xpath("//img[@value='print']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 18. Run Method Logout&Login
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = userName;
    methodlogoutlogin.password = password;
    methodlogoutlogin.execute(driver);

    // 19. Move mouse to 'moveUstUyeYonetimi'
    by = By.cssSelector("li[class='content']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 20. Click 'moveUstUyeYonetimi'
    by = By.cssSelector("li[class='content']");
    driver.findElement(by).click();

    // 21. Click 'altUyeYonetimi'
    by = By.xpath("//li[1]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();

    // 22. Is 'btnListele' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 23. Type 'c754@musteri.com' in 'SearchVendorName'
    by = By.cssSelector("#SearchVendorName");
    driver.findElement(by).sendKeys("c754@musteri.com");

    // 24. Click 'resultListelenen'
    by = By.xpath("//body/ul//div");
    driver.findElement(by).click();

    // 25. Click 'btnListele'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 26. Is 'Refresh' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.cssSelector("a[class='t-icon t-refresh']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 27. Is 'ilkSatırda c754@musteri.com' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    by = By.xpath("//tr[1]/td[contains(text(),'c754@musteri.com')]");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 28. Click 'EditButton'
    by = By.cssSelector("img[src*='fugue/edit.png']");
    driver.findElement(by).click();

    // 29. Select options in 'Language' with text '[NONE]'
    selectOptionbyVisibleText = WebExtensions.selectOptionbyVisibleText("English");
    by = By.cssSelector("#Entity_LanguageId");
    selectOptionbyVisibleText = (WebExtensions.SelectOptionbyVisibleText)((ReportingDriver)driver).addons().execute(selectOptionbyVisibleText, by, -1);

    // 30. Run Click Until Gone
    clickUntilGone = NetahsilatUtills.clickUntilGone(30);
    by = By.xpath("//button[. = 'Tamam']");
    clickUntilGone = (NetahsilatUtills.ClickUntilGone)((ReportingDriver)driver).addons().execute(clickUntilGone, by, -1);

    // 31. Click 'EditButton'
    by = By.cssSelector("img[src*='fugue/edit.png']");
    driver.findElement(by).click();

    // 32. Retrieves the text of the currently selected option of a select element
    getTextFromSelectedItem = WebListOperations.getGetTextFromSelectedItem();
    by = By.cssSelector("#Entity_LanguageId");
    getTextFromSelectedItem = (WebListOperations.GetTextFromSelectedItem)((ReportingDriver)driver).addons().execute(getTextFromSelectedItem, by, -1);

    // 33. Run Method Logout&Login
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = "c754@musteri.com";
    methodlogoutlogin.password = String.format("%s123",ProjectParameters.genelPassword);
    methodlogoutlogin.execute(driver);

    // 34. Is 'English Kullanıcı Sözleşmesi' visible?
    by = By.xpath("//p[normalize-space()='English Kullanıcı Sözleşmesi']");
    driver.findElement(by).isDisplayed();

    // 35. Click 'ConfirmationLoginAgreementForm'
    by = By.cssSelector("#ConfirmationLoginAgreementForm");
    driver.findElement(by).click();

    // 36. Click 'input[@type='submit' and @class='btn']'
    by = By.xpath("//input[@type='submit' and @class='btn']");
    driver.findElement(by).click();

    // 37. Is 'PureAmount' visible?
    by = By.cssSelector("#PureAmount");
    driver.findElement(by).isDisplayed();

    // 38. Run Method Logout&Login
    //    case dışına alındı
    /* Step is disabled
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = userName;
    methodlogoutlogin.password = password;
    methodlogoutlogin.execute(driver);
    */
    // 39. Run Method Üye Silme
    /* Step is disabled
    methodyesilme = new MethodyeSilme();
    methodyesilme.arananUye = "c754@musteri.com";
    methodyesilme.execute(driver);
    */
  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon.netahsilatdemo.com/Account/LogOff","otomasyon@test.com","Kd!cyhz4"));
  }
}
