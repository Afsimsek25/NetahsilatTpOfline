package netahsilat;

import io.testproject.addon.CombinedActions;
import io.testproject.addon.TabActions;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C3235
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Sun Oct 09 19:31:42 GMT 2022.
 */
@DisplayName("C3235")
public class C3235 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C3235")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String password) throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogin methodlogin;
    CombinedActions.ClearAndTypeAction clearAndTypeAction;
    TabActions.CloseLastTab closeLastTab;

    // 1. Navigate to '{{Otomasyon3Logout}}'
    //    Navigates the specified URL (Auto-generated)
    driver.navigate().to(ProjectParameters.Otomasyon3Logout);

    // 2. Run Method Login
    methodlogin = new MethodLogin();
    methodlogin.ApplicationURL = ApplicationURL;
    methodlogin.userName = "bireyselanabayi@otomasyon.com";
    methodlogin.password = ProjectParameters.genelPassword;
    methodlogin.execute(driver);

    // 3. Is 'KART KAYDI LİNKİ' visible?
    by = By.xpath("//a[. = 'KART KAYDI LİNKİ']");
    driver.findElement(by).isDisplayed();

    // 4. Click 'YÖNETİCİ'
    by = By.xpath("//a[. = 'YÖNETİCİ']");
    driver.findElement(by).click();

    // 5. Move mouse to 'moveToIslemler'
    by = By.cssSelector("li[class='tag']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 6. Click 'İŞLEMLER'
    by = By.cssSelector("a[title='İŞLEMLER']");
    driver.findElement(by).click();

    // 7. Is 'KART KAYDETME LINKI LISTELEME' visible?
    by = By.xpath("//a[. = 'Kart Kaydetme Linki Listeleme']");
    driver.findElement(by).isDisplayed();

    // 8. Move mouse to 'moveUstUyeYonetimi'
    by = By.cssSelector("li[class='content']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 9. Click 'ÜYE YÖNETİMİ2'
    by = By.xpath("//li[5]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();

    // 10. Click 'altUyeYonetimi'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(25000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(1000);
    by = By.xpath("//li[1]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 11. Type 'bireyselaltbayi@otomasyon.com' in 'SearchVendorName'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(2000);
    by = By.cssSelector("#SearchVendorName");
    driver.findElement(by).sendKeys("bireyselaltbayi@otomasyon.com");
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 12. Click 'resultListelenen'
    by = By.xpath("//body/ul//div");
    driver.findElement(by).click();

    // 13. Click 'Listele'
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();

    // 14. Is 'Refresh' visible?
    by = By.cssSelector("a[class='t-icon t-refresh']");
    driver.findElement(by).isDisplayed();

    // 15. Click 'Ödeme1'
    by = By.xpath("//a[. = 'Ödeme']");
    driver.findElement(by).click();

    // 16. Switch to window '1'
    driver.switchTo().window(driver.getWindowHandles().toArray(new String[0])[1]);

    // 17. Is 'Bireysel Alt Bayi (Silmeyiniz-Değişti...' visible?
    by = By.xpath("//strong[. = 'Bireysel Alt Bayi (Silmeyiniz-Değiştirmeyiniz)']");
    driver.findElement(by).isDisplayed();

    // 18. Is 'KART KAYDI LİNKİ' visible?
    by = By.xpath("//a[. = 'KART KAYDI LİNKİ']");
    driver.findElement(by).isDisplayed();

    // 19. Switch to window '0'
    driver.switchTo().window(driver.getWindowHandles().toArray(new String[0])[0]);

    // 20. Close the last tab opened in the browser
    closeLastTab = TabActions.getCloseLastTab();
    closeLastTab = (TabActions.CloseLastTab)((ReportingDriver)driver).addons().execute(closeLastTab);

    // 21. Run Clear 'SearchVendorName' contents and type 'bireyselaltmusteri@otomasyon.com'
    clearAndTypeAction = CombinedActions.clearAndTypeAction("bireyselaltmusteri@otomasyon.com");
    by = By.cssSelector("#SearchVendorName");
    clearAndTypeAction = (CombinedActions.ClearAndTypeAction)((ReportingDriver)driver).addons().execute(clearAndTypeAction, by, -1);

    // 22. Click 'resultListelenen'
    by = By.xpath("//body/ul//div");
    driver.findElement(by).click();

    // 23. Click 'Listele'
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();

    // 24. Is 'Refresh' visible?
    by = By.cssSelector("a[class='t-icon t-refresh']");
    driver.findElement(by).isDisplayed();

    // 25. Click 'Ödeme1'
    by = By.xpath("//a[. = 'Ödeme']");
    driver.findElement(by).click();

    // 26. Switch to window '1'
    driver.switchTo().window(driver.getWindowHandles().toArray(new String[0])[1]);

    // 27. Is 'Bireysel Alt Müşteri (Silmeyiniz-Deği...' visible?
    by = By.xpath("//strong[. = 'Bireysel Alt Müşteri  (Silmeyiniz-Değiştirmeyiniz)']");
    driver.findElement(by).isDisplayed();

    // 28. Is 'KART KAYDI LİNKİ' visible?
    by = By.xpath("//a[. = 'KART KAYDI LİNKİ']");
    driver.findElement(by).isDisplayed();

    // 29. Switch to window '1'
    driver.switchTo().window(driver.getWindowHandles().toArray(new String[0])[1]);

    // 30. Close the last tab opened in the browser
    closeLastTab = TabActions.getCloseLastTab();
    closeLastTab = (TabActions.CloseLastTab)((ReportingDriver)driver).addons().execute(closeLastTab);

    // 31. Navigate to 'https://otomasyon3.netahsilatdemo.com/Account/LogOff'
    driver.navigate().to("https://otomasyon3.netahsilatdemo.com/Account/LogOff");

    // 32. Run Method Login
    methodlogin = new MethodLogin();
    methodlogin.ApplicationURL = ApplicationURL;
    methodlogin.userName = "bireyselaltbayi@otomasyon.com";
    methodlogin.password = password;
    methodlogin.execute(driver);

    // 33. Is 'KART KAYDI LİNKİ' not present or invisible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//a[. = 'KART KAYDI LİNKİ']");
    (new WebDriverWait(driver, 2)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);

    // 34. Click 'YÖNETİCİ1'
    by = By.xpath("//a[. = 'YÖNETİCİ']");
    driver.findElement(by).click();

    // 35. Move mouse to 'moveToIslemler'
    by = By.cssSelector("li[class='tag']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 36. Click 'İŞLEMLER'
    by = By.cssSelector("a[title='İŞLEMLER']");
    driver.findElement(by).click();

    // 37. Is 'KART KAYDETME LINKI LISTELEME1' not present or invisible?
    by = By.xpath("//a[. = 'Kart Kaydetme Linki Listeleme']");
    (new WebDriverWait(driver, 2)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));

    // 38. Navigate to 'https://otomasyon3.netahsilatdemo.com/Account/LogOff'
    driver.navigate().to("https://otomasyon3.netahsilatdemo.com/Account/LogOff");

    // 39. Run Method Login
    methodlogin = new MethodLogin();
    methodlogin.ApplicationURL = ApplicationURL;
    methodlogin.userName = "bireyselaltmusteri@otomasyon.com";
    methodlogin.password = password;
    methodlogin.execute(driver);

    // 40. Is 'KART KAYDI LİNKİ' not present or invisible?
    by = By.xpath("//a[. = 'KART KAYDI LİNKİ']");
    (new WebDriverWait(driver, 2)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon3.netahsilatdemo.com/","Kd!cyhz4"));
  }
}
