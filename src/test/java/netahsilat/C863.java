package netahsilat;

import io.testproject.addon.JSActions;
import io.testproject.addon.WebExtensions;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C863 Bayi Limiti aynı gün işlem iptali ve geçmiş işlem iadesi ile denenmeli iptal yansımalı iade yansımamalı
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Thu Oct 06 10:51:31 GMT 2022.
 */
@DisplayName("C863 Bayi Limiti aynı gün işlem iptali ve geçmiş işlem iadesi ile denenmeli iptal yansımalı iade yansımamalı")
public class C863 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C863 Bayi Limiti aynı gün işlem iptali ve geçmiş işlem iadesi ile denenmeli iptal yansımalı iade yansımamalı")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String userName, String password, String kartNo, String CVV,
      String ayYil, String odemeLinki) throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    MethodParametrikdeme methodparametrikdeme;
    boolean booleanResult;
    MethodLogoutlogin methodlogoutlogin;
    WebExtensions.WaitForAlertPresence waitForAlertPresence;
    JSActions.ClickAction clickAction;
    WebExtensions.GetElementAttribute getElementAttribute;
    WebExtensions.AcceptAlert acceptAlert;
    WebExtensions.GetAlertText getAlertText;

    // 1. Run Method Logout&Login
    GeneratedUtils.sleep(500);
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = ProjectParameters.genelEmail;
    methodlogoutlogin.password = ProjectParameters.genelPassword;
    methodlogoutlogin.execute(driver);

    // 2. Move mouse to 'moveUstUyeYonetimi'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("li[class='content']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 3. Click 'ustUyeYonetimi'
    GeneratedUtils.sleep(500);
    by = By.xpath("//li[4]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();

    // 4. Click 'altUyeYonetimi'
    GeneratedUtils.sleep(500);
    by = By.xpath("//li[1]/a[. = 'ÜYE YÖNETİMİ']");
    driver.findElement(by).click();

    // 5. Type 'netahsilatlimitli@yandex.com' in 'SearchVendorName'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#SearchVendorName");
    driver.findElement(by).sendKeys("netahsilatlimitli@yandex.com");

    // 6. Click 'resultListelenen'
    GeneratedUtils.sleep(500);
    by = By.xpath("//body/ul//div");
    driver.findElement(by).click();

    // 7. Click 'btnListele'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 8. Is 'Refresh' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    by = By.cssSelector("a[class='t-icon t-refresh']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 9. Is 'netahsilatlimitli@yandex.com' visible?
    GeneratedUtils.sleep(500);
    by = By.xpath("//td[. = 'netahsilatlimitli@yandex.com']");
    driver.findElement(by).isDisplayed();

    // 10. Get 'href' attribute value on 'Ödeme'
    GeneratedUtils.sleep(500);
    getElementAttribute = WebExtensions.getElementAttribute("href");
    by = By.xpath("//a[. = 'Ödeme']");
    getElementAttribute = (WebExtensions.GetElementAttribute)((ReportingDriver)driver).addons().execute(getElementAttribute, by, -1);
    odemeLinki = String.valueOf(getElementAttribute.attributeValue);

    // 11. Navigate to '{{odemeLinki}}'
    GeneratedUtils.sleep(500);
    driver.navigate().to(odemeLinki);

    // 12. Run Method Parametrik Ödeme
    GeneratedUtils.sleep(500);
    methodparametrikdeme = new MethodParametrikdeme();
    methodparametrikdeme.ApplicationURL = "https://otomasyon.netahsilatdemo.com/Account/LogOff";
    methodparametrikdeme.kartNo = "4355084355084358";
    methodparametrikdeme.CVV = "000";
    methodparametrikdeme.ayYil = "12/30";
    methodparametrikdeme.execute(driver);

    // 13. Clear 'PureAmount' contents
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#PureAmount");
    driver.findElement(by).clear();

    // 14. Type '801' in 'PureAmount'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#PureAmount");
    driver.findElement(by).sendKeys("801");

    // 15. Click 'Taksit Seçimi'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#installment-selection-text");
    driver.findElement(by).click();

    // 16. Is '801,00 ₺' visible?
    GeneratedUtils.sleep(500);
    by = By.xpath("//div[. = '801,00 ₺']");
    driver.findElement(by).isDisplayed();

    // 17. Click 'AXESS Tek Çekim Posu'
    GeneratedUtils.sleep(500);
    clickAction = JSActions.getClickAction();
    by = By.cssSelector("td[class*='commRate-20'] input");
    clickAction = (JSActions.ClickAction)((ReportingDriver)driver).addons().execute(clickAction, by, -1);

    // 18. Click 'odemeTalimatiOlustur'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("input[value='ÖDEME TALİMATI OLUŞTUR']");
    driver.findElement(by).click();

    // 19. Click 'btnNormalPayment'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#btnNormalPayment");
    driver.findElement(by).click();

    // 20. Does 'txtReferansNumarasi' contain 'İşleminiz Tamamlandı'?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    by = By.xpath("(//p[@class='payment-result-text'])[1]");
    Assertions.assertTrue(driver.findElement(by).getText().contains("İşleminiz Tamamlandı"));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 21. Clear 'PureAmount' contents
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#PureAmount");
    driver.findElement(by).clear();

    // 22. Type '300' in 'PureAmount'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#PureAmount");
    driver.findElement(by).sendKeys("300");

    // 23. Click 'Taksit Seçimi'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#installment-selection-text");
    driver.findElement(by).click();

    // 24. Run Method Parametrik Ödeme
    GeneratedUtils.sleep(500);
    methodparametrikdeme = new MethodParametrikdeme();
    methodparametrikdeme.ApplicationURL = "https://otomasyon.netahsilatdemo.com/Account/LogOff";
    methodparametrikdeme.kartNo = "4355084355084358";
    methodparametrikdeme.CVV = "000";
    methodparametrikdeme.ayYil = "12/30";
    methodparametrikdeme.execute(driver);

    // 25. Is '300,00 ₺' visible?
    GeneratedUtils.sleep(500);
    by = By.xpath("//div[. = '300,00 ₺']");
    driver.findElement(by).isDisplayed();

    // 26. Click 'AXESS Tek Çekim Posu'
    GeneratedUtils.sleep(500);
    clickAction = JSActions.getClickAction();
    by = By.cssSelector("td[class*='commRate-20'] input");
    clickAction = (JSActions.ClickAction)((ReportingDriver)driver).addons().execute(clickAction, by, -1);

    // 27. Click 'odemeTalimatiOlustur'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("input[value='ÖDEME TALİMATI OLUŞTUR']");
    driver.findElement(by).click();

    // 28. Is 'Girmiş olduğunuz tutar ile GÜNLÜK işl...1' visible?
    GeneratedUtils.sleep(500);
    by = By.xpath("//li[. = 'Girmiş olduğunuz tutar ile GÜNLÜK işlem limitini geçiyorsunuz.']");
    driver.findElement(by).isDisplayed();

    // 29. Navigate to 'https://otomasyon.netahsilatdemo.com/Admin/Home'
    GeneratedUtils.sleep(500);
    driver.navigate().to("https://otomasyon.netahsilatdemo.com/Admin/Home");

    // 30. Click 'İŞLEM LİSTESİ'
    GeneratedUtils.sleep(500);
    clickAction = JSActions.getClickAction();
    by = By.cssSelector("a[title='İŞLEM LİSTESİ']");
    clickAction = (JSActions.ClickAction)((ReportingDriver)driver).addons().execute(clickAction, by, -1);

    // 31. Type 'netahsilatlimitli@yandex.com' in 'VendorName'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#VendorName");
    driver.findElement(by).sendKeys("netahsilatlimitli@yandex.com");

    // 32. Click '_Limitli Bayi'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    by = By.xpath("//div[contains(text(),'netahsilatlimitli@yandex.com')] ");
    driver.findElement(by).click();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 33. Click 'btnListele'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 34. Is 'Refresh' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    by = By.cssSelector("a[class='t-icon t-refresh']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 35. Click 'imgİptal'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("img[src*='fugue/cross-circle.png']");
    driver.findElement(by).click();

    // 36. Click 'btnİptalFromGrid'
    GeneratedUtils.sleep(500);
    by = By.xpath("//tr[1]/td[1]/a[1]/img");
    driver.findElement(by).click();

    // 37. Click 'buttonKaydiTamamla'
    GeneratedUtils.sleep(500);
    by = By.xpath("//button[. = 'Tamam']");
    driver.findElement(by).click();

    // 38. Is 'txtIslemYapiliyor' visible?
    GeneratedUtils.sleep(500);
    by = By.xpath("//*[text()='İşlem yapılıyor. Lütfen bekleyiniz...']");
    driver.findElement(by).isDisplayed();

    // 39. Is alert displayed?
    GeneratedUtils.sleep(500);
    waitForAlertPresence = WebExtensions.waitForAlertPresence(70000);
    waitForAlertPresence = (WebExtensions.WaitForAlertPresence)((ReportingDriver)driver).addons().execute(waitForAlertPresence);

    // 40. Get alert text
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    getAlertText = WebExtensions.getGetAlertText();
    getAlertText = (WebExtensions.GetAlertText)((ReportingDriver)driver).addons().execute(getAlertText);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 41. Accept alert
    GeneratedUtils.sleep(500);
    acceptAlert = WebExtensions.acceptAlert(50000);
    acceptAlert = (WebExtensions.AcceptAlert)((ReportingDriver)driver).addons().execute(acceptAlert);

    // 42. Navigate to '{{odemeLinki}}'
    GeneratedUtils.sleep(500);
    driver.navigate().to(odemeLinki);

    // 43. Clear 'PureAmount' contents
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#PureAmount");
    driver.findElement(by).clear();

    // 44. Type '300' in 'PureAmount'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#PureAmount");
    driver.findElement(by).sendKeys("300");

    // 45. Run Method Parametrik Ödeme
    GeneratedUtils.sleep(500);
    methodparametrikdeme = new MethodParametrikdeme();
    methodparametrikdeme.ApplicationURL = "https://otomasyon.netahsilatdemo.com/Account/LogOff";
    methodparametrikdeme.kartNo = "4355084355084358";
    methodparametrikdeme.CVV = "000";
    methodparametrikdeme.ayYil = "12/30";
    methodparametrikdeme.execute(driver);

    // 46. Is '300,00 ₺' visible?
    GeneratedUtils.sleep(500);
    by = By.xpath("//div[. = '300,00 ₺']");
    driver.findElement(by).isDisplayed();

    // 47. Click 'AXESS Tek Çekim Posu'
    GeneratedUtils.sleep(500);
    clickAction = JSActions.getClickAction();
    by = By.cssSelector("td[class*='commRate-20'] input");
    clickAction = (JSActions.ClickAction)((ReportingDriver)driver).addons().execute(clickAction, by, -1);

    // 48. Click 'odemeTalimatiOlustur'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("input[value='ÖDEME TALİMATI OLUŞTUR']");
    driver.findElement(by).click();

    // 49. Click 'btnNormalPayment'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#btnNormalPayment");
    driver.findElement(by).click();

    // 50. Does 'txtReferansNumarasi' contain 'İşleminiz Tamamlandı'?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    by = By.xpath("(//p[@class='payment-result-text'])[1]");
    Assertions.assertTrue(driver.findElement(by).getText().contains("İşleminiz Tamamlandı"));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 51. Navigate to 'https://otomasyon.netahsilatdemo.com/Admin/Home'
    GeneratedUtils.sleep(500);
    driver.navigate().to("https://otomasyon.netahsilatdemo.com/Admin/Home");

    // 52. Click 'İŞLEM LİSTESİ'
    GeneratedUtils.sleep(500);
    clickAction = JSActions.getClickAction();
    by = By.cssSelector("a[title='İŞLEM LİSTESİ']");
    clickAction = (JSActions.ClickAction)((ReportingDriver)driver).addons().execute(clickAction, by, -1);

    // 53. Type 'netahsilatlimitli@yandex.com' in 'VendorName'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("#VendorName");
    driver.findElement(by).sendKeys("netahsilatlimitli@yandex.com");

    // 54. Click '_Limitli Bayi'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    by = By.xpath("//div[contains(text(),'netahsilatlimitli@yandex.com')] ");
    driver.findElement(by).click();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 55. Click 'btnListele'
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    by = By.xpath("//button[. = 'Listele']");
    driver.findElement(by).click();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 56. Is 'Refresh' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    by = By.cssSelector("a[class='t-icon t-refresh']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 57. Click 'imgİptal'
    GeneratedUtils.sleep(500);
    by = By.cssSelector("img[src*='fugue/cross-circle.png']");
    driver.findElement(by).click();

    // 58. Click 'buttonKaydiTamamla'
    GeneratedUtils.sleep(500);
    by = By.xpath("//button[. = 'Tamam']");
    driver.findElement(by).click();

    // 59. Is 'txtIslemYapiliyor' visible?
    GeneratedUtils.sleep(500);
    by = By.xpath("//*[text()='İşlem yapılıyor. Lütfen bekleyiniz...']");
    driver.findElement(by).isDisplayed();

    // 60. Is alert displayed?
    GeneratedUtils.sleep(500);
    waitForAlertPresence = WebExtensions.waitForAlertPresence(50000);
    waitForAlertPresence = (WebExtensions.WaitForAlertPresence)((ReportingDriver)driver).addons().execute(waitForAlertPresence);

    // 61. Get alert text
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(35000, TimeUnit.MILLISECONDS);
    GeneratedUtils.sleep(500);
    getAlertText = WebExtensions.getGetAlertText();
    getAlertText = (WebExtensions.GetAlertText)((ReportingDriver)driver).addons().execute(getAlertText);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 62. Accept alert
    GeneratedUtils.sleep(500);
    acceptAlert = WebExtensions.acceptAlert(50000);
    acceptAlert = (WebExtensions.AcceptAlert)((ReportingDriver)driver).addons().execute(acceptAlert);

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon.netahsilatdemo.com/Account/LogOff","","","","","",""));
  }
}
