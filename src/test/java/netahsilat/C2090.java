package netahsilat;

import io.testproject.addon.VisibleElementsOperations;
import io.testproject.addon.WebExtensions;
import io.testproject.sdk.drivers.ReportingDriver;
import io.testproject.sdk.drivers.web.RemoteWebDriver;
import io.testproject.sdk.interfaces.junit5.ExceptionsReporter;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.MethodSource;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.util.concurrent.TimeUnit;
import java.util.stream.Stream;

/**
 * This class was automatically generated by TestProject
 * Project: Quality Museum Project
 * Test: C2090 Açıklama, email, telefon, Müşteri ödemelerinde TCKN alınsın parametrelerinin zorunluluğu kontrolü
 * Generated by: Ahmet Furkan SIMSEK (furkan.simsek@qualitymuseum.com)
 * Generated on Fri Oct 07 11:11:22 GMT 2022.
 */
@DisplayName("C2090 Açıklama, email, telefon, Müşteri ödemelerinde TCKN alınsın parametrelerinin zorunluluğu kontrolü")
public class C2090 implements ExceptionsReporter {
  public static WebDriver driver;

  @BeforeAll
  static void setup() throws Exception {
    driver = new RemoteWebDriver("wThDxIHw4zNaCydjAhcfe81WrQPYlMBFI7FPwUwV8M41", new ChromeOptions(), "Ahmet Furkan SIMSEK");
  }

  /**
   * In order to upload the test to TestProject need to un-comment @ArgumentsSource and set in comment the @MethodSource
   * @org.junit.jupiter.params.provider.ArgumentsSource(io.testproject.sdk.interfaces.parameterization.TestProjectParameterizer.class) */
  @DisplayName("C2090 Açıklama, email, telefon, Müşteri ödemelerinde TCKN alınsın parametrelerinin zorunluluğu kontrolü")
  @ParameterizedTest
  @MethodSource("provideParameters")
  void execute(String ApplicationURL, String userName, String password) throws Exception {
    // set timeout for driver actions (similar to step timeout)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    By by;
    boolean booleanResult;
    MethodLogoutlogin methodlogoutlogin;
    WebExtensions.GetElementAttribute getElementAttribute;
    VisibleElementsOperations.ClickIfVisibleWeb clickIfVisibleWeb;

    // 1. Run Method Logout&Login
    GeneratedUtils.sleep(500);
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = ProjectParameters.genelEmail;
    methodlogoutlogin.password = ProjectParameters.genelPassword;
    methodlogoutlogin.execute(driver);

    // 2. Move mouse to 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 3. Click 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    driver.findElement(by).click();

    // 4. Click 'btnParametreler'
    by = By.cssSelector("a[title='PARAMETRELER']");
    driver.findElement(by).click();

    // 5. Is 'Ödemelerde BIN numarası kontrol edils...' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    by = By.xpath("//label[. = 'Ödemelerde BIN numarası kontrol edilsin']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 6. Click 'li > a > Ödeme Formu Ayarları'
    by = By.xpath("//a[normalize-space()='Ödeme Formu Ayarları']");
    driver.findElement(by).click();

    // 7. Is 'Taksit Tablosu Değişiklik Uyarıları' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    by = By.xpath("//legend[. = 'Taksit Tablosu Değişiklik Uyarıları']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 8. Click 'Telefon Zorunlu (Checked)- Ödeme Formu Ayarları' if it's visible
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("");
    by = By.xpath("//input[@id='PaymentFormFieldSetting_IsPhoneRequired']//.. //span[@class='jquery-safari-checkbox']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 9. Is 'Telefon Zorunlu (Unchecked) -Ödeme Formu Ayarları' visible?
    by = By.xpath("//input[@id='PaymentFormFieldSetting_IsPhoneRequired']//.. //span[@class='jquery-safari-checkbox jquery-safari-checkbox-checked']");
    driver.findElement(by).isDisplayed();

    // 10. Click 'E-posta Zorunlu (Checked) -Ödeme Formu Ayarları' if it's visible
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("");
    by = By.xpath("//input[@id='PaymentFormFieldSetting_IsEMailRequired']//.. //span[@class='jquery-safari-checkbox']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 11. Is 'E-posta Zorunlu (Unchecked) -Ödeme Formu Ayarları' visible?
    by = By.xpath("//input[@id='PaymentFormFieldSetting_IsEMailRequired']//.. //span[@class='jquery-safari-checkbox jquery-safari-checkbox-checked']");
    driver.findElement(by).isDisplayed();

    // 12. Click 'Açıklama Zorunlu (Checked) -Ödeme Formu Ayarları' if it's visible
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("");
    by = By.xpath("//input[@id='PaymentFormFieldSetting_IsDescriptionRequired']//.. //span[@class='jquery-safari-checkbox']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 13. Is 'Açıklama Zorunlu (Unchecked) -Ödeme Formu Ayarları' visible?
    by = By.xpath("//input[@id='PaymentFormFieldSetting_IsDescriptionRequired']//.. //span[@class='jquery-safari-checkbox jquery-safari-checkbox-checked']");
    driver.findElement(by).isDisplayed();

    // 14. Click 'Müşteri ödemelerinde TCKN alınsın Zorunlu mu (Checked)' if it's visible
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("");
    by = By.xpath("//input[@name='GetTCKNCustomerPaymentRequired']//.. //span[@class='jquery-safari-checkbox']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 15. Is 'Müşteri ödemelerinde TCKN alınsın Zorunlu (UnChecked)' visible?
    by = By.xpath("//input[@name='GetTCKNCustomerPaymentRequired']//.. //span[@class='jquery-safari-checkbox jquery-safari-checkbox-checked']");
    driver.findElement(by).isDisplayed();

    // 16. Click 'Kaydet1'
    by = By.cssSelector("#kaydet");
    driver.findElement(by).click();

    // 17. Does 'Ödeme Formu Ayarları Başarıyla kayded...' contain 'Ödeme Formu Ayarları Başarıyla kaydedildi.'?
    by = By.xpath("//li[. = 'Ödeme Formu Ayarları Başarıyla kaydedildi.']");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Ödeme Formu Ayarları Başarıyla kaydedildi."));

    // 18. Run Method Logout&Login
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = "netahsilatotomasyon@yandex.com";
    methodlogoutlogin.password = password;
    methodlogoutlogin.execute(driver);

    // 19. Click 'ONLINE ÖDEME'
    by = By.xpath("//a[.='ONLINE ÖDEME']");
    driver.findElement(by).click();

    // 20. Move mouse to 'Ödeme Talimatı Olustur '
    by = By.cssSelector("input[class='span10 btn pull-right btn-warning']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 21. Get 'style' attribute value on 'Telefon - Zorunlu mu işareti'
    getElementAttribute = WebExtensions.getElementAttribute("style");
    by = By.xpath("//label[normalize-space()='Telefon*']//span[@class='validater']");
    getElementAttribute = (WebExtensions.GetElementAttribute)((ReportingDriver)driver).addons().execute(getElementAttribute, by, -1);

    // 22. Get 'style' attribute value on 'E-Mail zorunlu mu işareti'
    getElementAttribute = WebExtensions.getElementAttribute("style");
    by = By.xpath("//label[normalize-space()='E-Mail*']//span[@class='validater']");
    getElementAttribute = (WebExtensions.GetElementAttribute)((ReportingDriver)driver).addons().execute(getElementAttribute, by, -1);

    // 23. Get 'style' attribute value on 'Açıklama zorunlu mu işareti'
    getElementAttribute = WebExtensions.getElementAttribute("style");
    by = By.xpath("//label[normalize-space()='Açıklama*']//span[@class='validater']");
    getElementAttribute = (WebExtensions.GetElementAttribute)((ReportingDriver)driver).addons().execute(getElementAttribute, by, -1);

    // 24. Get 'style' attribute value on 'TC Kimlik No zorunlu mu işareti'
    getElementAttribute = WebExtensions.getElementAttribute("style");
    by = By.xpath("//label[normalize-space()='TC Kimlik No*']//span[@class='validater']");
    getElementAttribute = (WebExtensions.GetElementAttribute)((ReportingDriver)driver).addons().execute(getElementAttribute, by, -1);

    // 25. Clear 'inputEmailPricinPage' contents
    by = By.cssSelector("#Email");
    driver.findElement(by).clear();

    // 26. Clear 'Telefon Numarası' contents
    by = By.cssSelector("#Phone");
    driver.findElement(by).clear();

    // 27. Click 'Ödeme Talimatı Oluştur Button'
    by = By.xpath("//input[@type='button' and @value='ÖDEME TALİMATI OLUŞTUR']");
    driver.findElement(by).click();

    // 28. Is 'Lütfen email giriniz' visible?
    by = By.xpath("//li[. = 'Lütfen email giriniz']");
    driver.findElement(by).isDisplayed();

    // 29. Is 'Telefon boş geçilemez3' visible?
    by = By.xpath("//li[. = 'Telefon boş geçilemez']");
    driver.findElement(by).isDisplayed();

    // 30. Is 'TC kimlik numarası boş geçilemez' visible?
    by = By.xpath("//li[. = 'TC kimlik numarası boş geçilemez']");
    driver.findElement(by).isDisplayed();

    // 31. Is 'Açıklama kısmı boş geçilemez' visible?
    by = By.xpath("//li[. = 'Açıklama kısmı boş geçilemez']");
    driver.findElement(by).isDisplayed();

    // 32. Run Method Logout&Login
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = userName;
    methodlogoutlogin.password = password;
    methodlogoutlogin.execute(driver);

    // 33. Move mouse to 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    (new Actions(driver)).moveToElement(driver.findElement(by)).perform();

    // 34. Click 'moveToSiteYönetimi'
    by = By.cssSelector("li[class='config']");
    driver.findElement(by).click();

    // 35. Click 'btnParametreler'
    by = By.cssSelector("a[title='PARAMETRELER']");
    driver.findElement(by).click();

    // 36. Is 'Ödemelerde BIN numarası kontrol edils...' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);
    by = By.xpath("//label[. = 'Ödemelerde BIN numarası kontrol edilsin']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 37. Click 'li > a > Ödeme Formu Ayarları'
    by = By.xpath("//a[normalize-space()='Ödeme Formu Ayarları']");
    driver.findElement(by).click();

    // 38. Is 'Taksit Tablosu Değişiklik Uyarıları' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//legend[. = 'Taksit Tablosu Değişiklik Uyarıları']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 39. Click 'Telefon Zorunlu (Unchecked) -Ödeme Formu Ayarları' if it's visible
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("");
    by = By.xpath("//input[@id='PaymentFormFieldSetting_IsPhoneRequired']//.. //span[@class='jquery-safari-checkbox jquery-safari-checkbox-checked']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);

    // 40. Is 'Telefon Zorunlu (Checked)- Ödeme Formu Ayarları' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//input[@id='PaymentFormFieldSetting_IsPhoneRequired']//.. //span[@class='jquery-safari-checkbox']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 41. Click 'E-posta Zorunlu (Unchecked) -Ödeme Formu Ayarları' if it's visible
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("");
    by = By.xpath("//input[@id='PaymentFormFieldSetting_IsEMailRequired']//.. //span[@class='jquery-safari-checkbox jquery-safari-checkbox-checked']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);

    // 42. Is 'E-posta Zorunlu (Checked) -Ödeme Formu Ayarları' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//input[@id='PaymentFormFieldSetting_IsEMailRequired']//.. //span[@class='jquery-safari-checkbox']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 43. Click 'Açıklama Zorunlu (Unchecked) -Ödeme Formu Ayarları' if it's visible
    clickIfVisibleWeb = VisibleElementsOperations.clickIfVisibleWeb("");
    by = By.xpath("//input[@id='PaymentFormFieldSetting_IsDescriptionRequired']//.. //span[@class='jquery-safari-checkbox jquery-safari-checkbox-checked']");
    clickIfVisibleWeb = (VisibleElementsOperations.ClickIfVisibleWeb)((ReportingDriver)driver).addons().execute(clickIfVisibleWeb, by, -1);

    // 44. Is 'Açıklama Zorunlu (Checked) -Ödeme Formu Ayarları' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//input[@id='PaymentFormFieldSetting_IsDescriptionRequired']//.. //span[@class='jquery-safari-checkbox']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 45. Click 'Müşteri ödemelerinde TCKN alınsın Zorunlu (UnChecked)'
    by = By.xpath("//input[@name='GetTCKNCustomerPaymentRequired']//.. //span[@class='jquery-safari-checkbox jquery-safari-checkbox-checked']");
    driver.findElement(by).click();

    // 46. Is 'Müşteri ödemelerinde TCKN alınsın Zorunlu mu (Checked)' visible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//input[@name='GetTCKNCustomerPaymentRequired']//.. //span[@class='jquery-safari-checkbox']");
    driver.findElement(by).isDisplayed();
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 47. Click 'Kaydet1'
    by = By.cssSelector("#kaydet");
    driver.findElement(by).click();

    // 48. Does 'Ödeme Formu Ayarları Başarıyla kayded...' contain 'Ödeme Formu Ayarları Başarıyla kaydedildi.'?
    by = By.xpath("//li[. = 'Ödeme Formu Ayarları Başarıyla kaydedildi.']");
    Assertions.assertTrue(driver.findElement(by).getText().contains("Ödeme Formu Ayarları Başarıyla kaydedildi."));

    // 49. Run Method Logout&Login
    methodlogoutlogin = new MethodLogoutlogin();
    methodlogoutlogin.ApplicationURL = ApplicationURL;
    methodlogoutlogin.userName = "netahsilatotomasyon@yandex.com";
    methodlogoutlogin.password = password;
    methodlogoutlogin.execute(driver);

    // 50. Click 'ONLINE ÖDEME'
    by = By.xpath("//a[.='ONLINE ÖDEME']");
    driver.findElement(by).click();

    // 51. Get 'style' attribute value on 'Telefon - Zorunlu mu işareti'
    getElementAttribute = WebExtensions.getElementAttribute("style");
    by = By.xpath("//label[normalize-space()='Telefon*']//span[@class='validater']");
    getElementAttribute = (WebExtensions.GetElementAttribute)((ReportingDriver)driver).addons().execute(getElementAttribute, by, -1);

    // 52. Get 'style' attribute value on 'E-Mail zorunlu mu işareti'
    getElementAttribute = WebExtensions.getElementAttribute("style");
    by = By.xpath("//label[normalize-space()='E-Mail*']//span[@class='validater']");
    getElementAttribute = (WebExtensions.GetElementAttribute)((ReportingDriver)driver).addons().execute(getElementAttribute, by, -1);

    // 53. Get 'style' attribute value on 'Açıklama zorunlu mu işareti'
    getElementAttribute = WebExtensions.getElementAttribute("style");
    by = By.xpath("//label[normalize-space()='Açıklama*']//span[@class='validater']");
    getElementAttribute = (WebExtensions.GetElementAttribute)((ReportingDriver)driver).addons().execute(getElementAttribute, by, -1);

    // 54. Get 'style' attribute value on 'TC Kimlik No zorunlu mu işareti'
    getElementAttribute = WebExtensions.getElementAttribute("style");
    by = By.xpath("//label[normalize-space()='TC Kimlik No*']//span[@class='validater']");
    getElementAttribute = (WebExtensions.GetElementAttribute)((ReportingDriver)driver).addons().execute(getElementAttribute, by, -1);

    // 55. Click 'Ödeme Talimatı Oluştur Button'
    by = By.xpath("//input[@type='button' and @value='ÖDEME TALİMATI OLUŞTUR']");
    driver.findElement(by).click();

    // 56. Is 'Lütfen email giriniz' not present or invisible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//li[. = 'Lütfen email giriniz']");
    (new WebDriverWait(driver, 2)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 57. Is 'Telefon boş geçilemez3' not present or invisible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//li[. = 'Telefon boş geçilemez']");
    (new WebDriverWait(driver, 2)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 58. Is 'TC kimlik numarası boş geçilemez' not present or invisible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//li[. = 'TC kimlik numarası boş geçilemez']");
    (new WebDriverWait(driver, 2)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

    // 59. Is 'Açıklama kısmı boş geçilemez' not present or invisible?
    // set step-specific timeout (will undo this at the end)
    driver.manage().timeouts().implicitlyWait(2000, TimeUnit.MILLISECONDS);
    by = By.xpath("//li[. = 'Açıklama kısmı boş geçilemez']");
    (new WebDriverWait(driver, 2)).until(ExpectedConditions.invisibilityOf(driver.findElement(by)));
    driver.manage().timeouts().implicitlyWait(15000, TimeUnit.MILLISECONDS);

  }

  @Override
  public ReportingDriver getDriver() {
    return (ReportingDriver) driver;
  }

  @AfterAll
  static void tearDown() {
    if (driver != null) {
      driver.quit();
    }
  }

  private static Stream provideParameters() throws Exception {
    return Stream.of(Arguments.of("https://otomasyon.netahsilatdemo.com/Account/LogOff","otomasyon@test.com","Kd!cyhz4"));
  }
}
